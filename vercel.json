{
  "buildCommand": "npm run test:regression && npm run build",
  "installCommand": "npm ci",
  "ignoreCommand": "if [ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ]; then exit 1; fi; ! git diff \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD --name-only | grep -qvE '^(docs/|.*\\.md$)'"
}
