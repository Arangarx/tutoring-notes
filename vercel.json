{
  "buildCommand": "npm run test:regression && npm run build",
  "installCommand": "npm ci",
  "ignoreCommand": "node scripts/vercel-ignore-build.cjs",
  "crons": [
    {
      "path": "/api/cron/transcribe-sweep",
      "schedule": "* * * * *"
    }
  ]
}
