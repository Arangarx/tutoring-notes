# Docs archive — cold storage

**This directory is COLD STORAGE.** It holds superseded or captured-elsewhere documentation moved out of the live `docs/` tree to reduce navigation clutter.

- **Not authoritative.** Nothing here should be acted on directly.
- **Agents:** do **not** explore, search, or cite files under `docs/archive/` unless Andrew explicitly requests an archive audit.
- **Humans:** use [`ARCHIVE-LEDGER.md`](ARCHIVE-LEDGER.md) for the audit trail (original path, bucket, canonical replacement).
- **Indexing:** `docs/archive/` is listed in `.cursorindexingignore` so semantic search skips this tree by default; files remain readable on explicit request.

If you followed a stale link from another doc, check the ledger row for the current canonical source.
