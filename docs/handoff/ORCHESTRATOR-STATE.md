# ORCHESTRATOR STATE â€” canonical living bootstrap

> **READ THIS FIRST.** Single source of current orchestrator state for tutoring-notes. A **brand-new orchestrator chat** reads this + its reading list + `git log` â€” **no catch-up from Andrew** on what's done, where we are, what's next, or how we work.
>
> **Operating contract** ([`.cursor/rules/orchestrator-discipline.mdc`](../../.cursor/rules/orchestrator-discipline.mdc)): state durability is a **primary reliability obligation**. Sessions can be lost at any moment; keep this file continuously current. Treat "I'll update state later" as a silent failure.

---

## HEAD

**ðŸš€ ACTING (2026-07-09 ~12:35 MT) â€” Sarah delivery path = merge `v1-redesign` â†’ `master`.** Andrew: Sarah has **only ever used master** (old UI); redesign preview is Andrew-only. Goal: land integrity fixes on `v1-redesign`, full gate green, then master cut so Sarah sees the new product. **HARD RULE (Andrew re-iterated):** anything mechanical/visual we touch MUST have Playwright â€” he must not be the regression net.

**Tip:** `v1-redesign` â€” AUDIO-1 attempt #4 merge pending (branch `fix/smoke-audio-1-attempt4-mutex-swap`).

**Last action:** Implemented attempt #4 â€” shared device mutex, tutor mic-before-cam, swap-aligned silent recovery.

**In-flight:** merging #4 â†’ Andrew Brio re-smoke.

**Next action(s):** Andrew cancelâ†’refresh Brio smoke. Filter `silent_track` for `userPickedSlot=true` / recovered vs unrecoverable. **No master cut while AUDIO-1 fails on Brio.** If #4 fails â†’ product mitigation (explicit re-arm), not another backoff.

**Evening smoke + #3 fail / #4 shipped:**
| Item | Result |
|------|--------|
| Admin icons | PASS |
| PDF stroke bleed E5 | Did not repro (watch) |
| Blob token / camOn | Watch |
| AUDIO-1 #3 | FAIL â€” unrecoverable ~5.8s / 3 slots |
| AUDIO-1 #4 | Shipped â€” needs hardware re-smoke |

**Decisions locked (2026-07-09 earlier):** SEC-POLICY=both; cancel=keep-delete + fresh id; interstitial=verify host; Finish review CTA; billing round-up; wordmark `/?view=home`; replay-tab was test oracle.

**âœ… Wave 1 COMPLETE â€” all 4 merged to `v1-redesign` tip `969ae7d` (docs `f9294ed`):**
1. notes prompt_wins `49485ea` ([notes](1eceaad8-da08-4417-8d58-54bc5d447d9e))
2. SEC-ERASURE-TUTOR-GATE `fd62272` ([SEC-ERASURE](ab6b4eca-23b0-4b9b-b4a3-eb955989be08))
3. View whiteboard â†’ new replay `a249897` ([view whiteboard](bc2b1f60-fb51-47fd-9a77-880d1c6a4c0a)) â€” tutor PW 2/2 green; **parent share PW red** (likely auth-wall: `/s/` without AH session) â†’ fix in flight [share PW auth](c3aba2c2-c575-4391-a97e-bbc2b3c7411a)
4. UX bundle `969ae7d` ([UX bundle](48187f62-85fb-4f7a-9e39-fe1de591be6f))

**âœ… Wave 2 COMPLETE:**
- claim set-up-later+escape `5b413a8` ([claim setup](379004a7-f398-4c8a-8bb2-4a46b33f0509))
- share replay PW auth `f4cc7ca` ([share PW](c3aba2c2-c575-4391-a97e-bbc2b3c7411a)) â€” 3/3 view-whiteboard PW green

**âœ… Wave 3 COMPLETE:**
- cancel + fresh session id `aa1da56` ([cancel + fresh id](3d87a590-f0ad-46be-b7e9-2bdc8f42832d) â†’ `a75988a`) â€” PENDING 404â†’`session_canceled`; `location.replace` + key clear; Playwright enrolled
- Finish review + SEC-POLICY copy `4d3e9f4` ([Finish review + policy](f971a66e-291b-49b8-84dc-5b06c0e90976) â†’ `30c340b`)
- replay-tab aria `508eefb` ([replay-tab aria](9d372870-f4bb-4dce-b14c-1c98bac8db45) â†’ `ec6b67b`) + oracle follow-up `a524211` ([replay activePageId](896736f4-f5f3-468c-a838-c9b90f0fd571) â†’ `a8870de`) â€” test-only; product `activeReplayPageId` was correct

**âœ… E4 PDF bleed (Andrew recheck FAIL â†’ fix same day):** `29b01d7` merge of `cd71cf8` â€” foreign-id guard on PDF boards after fingerprint clear.

**Historical (collapsed):** Bug A `27d6cc5`, wave5â†’v1-redesign merge `d6b4433`, smoke triage trail below remains for audit until heavy restructure.

- **ðŸ” SEC-SHARE-WALL (Andrew smoke 2026-07-08, flagged MAJOR) â€” `/s/[token]` serves student notes ANONYMOUSLY to any valid-token holder. ROOT CAUSE = dormant flag, NOT broken access-control.** Investigation [explore](c73715d9-82ea-46d8-8ec2-9013759a2695): the full auth wall IS built + correct (`share-access-scope.ts`, commit `fd2ccaf`); anon access happens because **`NOTES_AUTH_WALL` is unset/false on v1-redesign preview** â†’ both `assertCanAccessShareLink` (`:108-116`, pages) and `checkApiShareAccess` (`:269-274`, APIs) take the `access_granted_anon_grace` branch = **token-only, no session, not time-bounded**. Per the ratified design ([`authed-session-access-design-2026-06-10.md`](authed-session-access-design-2026-06-10.md) Â§1.1/Â§3.2/Â§3.5.3) this was **shipped dormant on purpose** â€” flag flips `true` ONLY at the v1-redesignâ†’master cutover, AFTER pilot families claimed. **Andrew rejected timed grace** ("no grace period at our scale"). Sarah's hard ask: notes must require login. **Blast radius (wall off):** logged-out stranger with the link sees student name, tutor name, ALL non-DRAFT notes (topics/assessment/plan/homework/links), audio (`/api/audio/[id]?token=`), + WB replay (public-events/snapshot/concat-audio APIs â€” all gated only by `checkApiShareAccess` â†’ same anon-grace). Token revocation + erasure-guard still work. **Reframes item 17:** with wall off, an unauthorized stranger and an entitled parent are INDISTINGUISHABLE (token = credential) â†’ item 17 "share access" behaved as designed-dormant; any empty-notes sub-issue is the separate save-nav/DRAFT thread. **The footgun:** secure state is opt-IN (must set flag) rather than default â†’ preview silently leaks. **âœ… DECIDED (Andrew 2026-07-09, "A"): option A â€” secure-by-default in code + drop anon-grace as prod/preview path.** **âœ… DONE ([Sonnet](4afb4a39-cb4c-4d0e-8e34-4efd7678eee6), branch `fix/sec-share-wall-secure-default` @ `d06901c`, pushed) â€” awaiting Andrew smoke.** Inverted `isNotesAuthWallEnabled()` â†’ wall ON unless `NOTES_AUTH_WALL` explicitly `false`/`0` (local-dev only) in `share-access-scope.ts` + `middleware.ts` `/s/*` fast-path; anon-grace no longer an access-granting outcome when wall ON. REDâ†’GREEN: 4 new BLOCKER assertions failed pre-fix (unset env â†’ allowed), pass post-fix; 8 existing grace tests updated to explicit `NOTES_AUTH_WALL=false` sentinel (7 files). jest 67/67 on share-access-scope, full suite 3097+ pass (3 pre-existing unrelated flakes). 5-axis: all 7 `/s/*` surfaces (pagesâ†’login-redirect, APIsâ†’401) closed; entitled AH/learner unchanged; revocation still runs first; residual = browser-cached pre-fix 200s expire naturally (not a gap). Preview: `https://tutoring-notes-git-fix-sec-share-130eed-arangarx-5209s-projects.vercel.app`. **To smoke:** logged-out + valid `/s/` link â†’ login redirect; entitled parent/learner â†’ still sees notes. **NO Vercel env write needed** â€” the Vercel MCP here has no env-var tool AND secure-by-default code makes preview/prod ON automatically; local `.env` has no `NOTES_AUTH_WALL` so nothing to remove. **âœ… MERGED into `v1-redesign` 2026-07-09 (`561d7a9`, pushed).** Order honored: P0 bleed first (`9c36cb1`), SEC second (`561d7a9`). Integrated tip passed `next build` (exit 0, middleware compiled). Andrew asked to consolidate both into v1-redesign so he can smoke everything together. (Prior options B/D not taken.) **Fix files DISJOINT from P0 WWC** (`share-access-scope.ts`, `middleware.ts`, share-scope tests, new Playwright, `tests/visual/pages.spec.ts`, `.env.example`) â†’ safe to serialize independently. **Flipping the preview env is an MCP WRITE (greenlight) + will lock Andrew out of share unless logged in as the entitled/claimed parent â€” do NOT flip mid-smoke without his go.** **Test coverage hole:** current `share-access-scope.test.ts` ASSERTS anon access is allowed when wall off (encodes the footgun as correct); `tests/visual/pages.spec.ts` REQUIRES anonymous share load; ZERO Playwright with `NOTES_AUTH_WALL=true`. Fix must add the wall-ON logged-out-denied E2E at page + API.
- **ðŸ” SEC-ERASURE-TUTOR-GATE (Andrew smoke 2026-07-08) â€” pending-erasure/purged student is NOT read-only-blocked on the tutor side; only session-start is gated + a banner that OVER-PROMISES.** Andrew: "nothing on `/admin/students/<id>` should be there when pending erasure/deleted â€” no notes access, audio upload, add notes, share link, anything; block it like starting a session." Investigation [explore](b001e10e-8675-4102-b4a2-bae5e96cbe0b). **The machinery ALREADY EXISTS â€” it's just applied inconsistently (defense-in-depth gap, NOT missing infra).** Erasure state: `LP/AH.tombstonedAt` set IMMEDIATELY at request (grace); `Student.erasedAt` set only at db_scrubbing (post-7-day-grace) â†’ **pending grace is detectable via tombstone + active `ErasureJob`, NOT `erasedAt`**; helpers exist: `isWhiteboardSessionBlockedByErasure` (tombstone-or-job, used by session create/start), `assertStudentNotErased`/`assertStudentNotErasedApi` (grace+purged, calls `notFound()`), `deriveStudentErasureDisplayState`/`isStudentAccessSuspended`, `isStudentContentAccessSuspended`. **ALREADY gated:** `createWhiteboardSession`+`startWhiteboardSession` (server, `whiteboard/actions.ts:128-137,304-313`), WB **review/replay** page (`[whiteboardSessionId]/page.tsx:106`), ALL share-consumer `/s/*` pages + content APIs (events/snapshot/concat/audio), end-session persist short-circuit. **GAPS (ownership-only, erasure UNguarded â€” Andrew's list):** (1) **student detail `page.tsx`** computes `accessSuspended` but only swaps the sticky-CTA + shows banner â€” ALL sections (notes form, share create/regenerate/revoke, session list Resume/End/Delete, claim) still render; passes `accessSuspended={false}` to `StartWhiteboardSession` (`:193-206`); NO `students/[id]/layout.tsx` exists. (2) **notes list `notes/page.tsx:41-61`** â€” ownership-only then reads + renders full note bodies = **Andrew's PRIMARY complaint**. (3) **note mutations `actions.ts`** `createNote`/`updateNote`/`deleteNote`/`setNoteStatus`/`generateNoteFromTextAction`/`transcribeAndGenerateAction` â€” all `assertOwnsStudent` only. (4) **WB notes pipeline `notes-actions.ts`** save/finalize/delete/regenerate â€” `assertOwnsWhiteboardSession` only. (5) **share mint** `regenerateShareLink` + `sendUpdateEmail` (auto-mints) â€” ownership only. (6) **audio upload** `POST /api/upload/audio:127`, `/api/upload/blob:155` (audio kind), `registerWhiteboardSessionAudioSegmentAction` â€” ownership only. (7) **workspace page** `workspace/page.tsx:70-74`, `issueJoinToken`, transcription enqueue, `renameStudent`, `deleteStudent`(non-erasure), `disconnectLearnerProfile`, **claim-invite mint** `POST /api/students/[id]/claim-invites:42` â€” all UNguarded. **âš ï¸ The banner LIES:** `StudentErasureStatus.tsx:78-83` says "You cannot start new sessions or view this student's content during the grace period" while the UI serves all of it. **FIX (Sonnet â€” auth/data-lifecycle boundary; do NOT implement till after P0):** BOTH layers â€” (a) **security:** add `assertStudentNotErased` right after `assertOwnsStudent` in every student-scoped mutation + upload-token mint + workspace page + claim mint (consider a `assertOwnsMutableStudent` wrapper in `student-scope.ts`; align the predicate with `isWhiteboardSessionBlockedByErasure` tombstone-parity so tutor gate == session-start gate); (b) **UX:** add `students/[id]/layout.tsx` (cleanest single choke) OR top-of-page short-circuit in `page.tsx`+`notes/page.tsx` â†’ when `isStudentAccessSuspended` render banner + minimal shell (roster link only), skip all sections; and call `assertStudentNotErased` on `notes/page.tsx` for defense. **Choose ONE unified `isStudentTutorAccessSuspended` predicate mirroring session-start** so grace/purged/edge-tombstone all match. Files: `student-scope.ts`, `active-erasure-scope.ts`, `assert-student-not-erased.ts`, NEW `students/[id]/layout.tsx`, `students/[id]/page.tsx`+`notes/page.tsx`, `actions.ts`, `whiteboard/actions.ts`+`notes-actions.ts`, `workspace/page.tsx`, `api/upload/audio+blob/route.ts`, `api/students/[id]/claim-invites/route.ts`, extend `erasure-access-suspension.integration.test.ts`. **NO P0 WWC overlap.** **Interaction w/ SEC-SHARE-WALL:** share CONSUMER already erasure-gated; this closes the tutor MINT + tutor-content side. **Test hole:** grace-state fixture (`requestErasureByAdmin` â†’ tombstone set, `erasedAt` null, active job) â†’ assert every action above blocks; controls: post-`cancelErasureJob` allows, post-purge blocks; ownershipÃ—erasure matrix; Playwright: suspended detail shows no notes/share/session affordances. **Priority: pre-Sarah, alongside SEC-SHARE-WALL (compliance-integrity); after P0 shared tree.** â†’ mirror to `docs/BACKLOG.md`.
- **ðŸ”´ðŸ” SEC-POLICY-TRUTH (Andrew smoke Q 2026-07-08 "are we TRULY honoring privacy/terms? is there a cron that detects the 24-mo retention state?") â€” NO. The privacy policy describes an automated data-lifecycle the codebase does NOT have. COMPLIANCE TRUTHFULNESS GAP â€” needs Andrew direction before Sarah (real minor data).** Full audit [explore](dfa5514b-b7e5-482f-9e42-7b9596fc96b9). **HEADLINE â€” 24-month COPPA Â§312.10 retention (privacy `page.tsx:250-261,358-367`): NO cron enforces it, and it's STRUCTURALLY unenforceable today** for 3 stacked reasons: (a) **no retention cron** â€” `vercel.json` has only `transcribe-sweep` + `erasure/process`; the erasure cron only ADVANCES operator-created `ErasureJob`s (SELECT-only, `process-erasure-batch.ts:27-38`), never ORIGINATES from a timer; jobs are created ONLY by `requestErasureByAdmin` (ADMIN-only, 7-day grace NOT 24-mo); (b) **no trigger timestamps** â€” schema has NO `closedAt`/`retentionExpiresAt`/`relationshipEndedAt` (only `tombstonedAt` = erasure); (c) **"account closed" is not a modeled state** â€” no self-service closure; `AccountHolder` has no `closedAt`, `AdminUser` no deactivation, disconnect just nulls the FK with no clock. â†’ the purge PIPELINE works; the policy CLOCK doesn't exist. **OTHER copy-vs-reality gaps (no mechanism = truthfulness risk):** (1) "we do not retain children's info indefinitely" â€” only true if an operator manually erases; (2) **"grade level" listed as collected** (`page.tsx:91,326-328`) but **NO grade field anywhere in `prisma/schema.prisma`**; (3) **educational-use consent + "tutor is notified" on revocation** (`page.tsx:402-410`) â€” NO `allowEducationalUse` in `ConsentRecord`, NO notification code; (4) **"short-lived signed URLs"** (`page.tsx:204-205`) â€” playback is actually an auth-gated server PROXY (security is FINE/arguably better, but wording inaccurate); (5) **tutor `deleteStudent` HARD-deletes immediately** (`actions.ts:810-813`) â€” CONTRADICTS the retention schedule from the other direction (may delete minor PII w/o parental-verification path); (6) parent deletion = email + manual operator only (no self-service, no SLA â€” ties to the self-service-account-deletion backlog item). **HONORED (the majority â€” reassurance):** private Blob + auth-gated playback (not public URLs), disclosed subprocessors w/ NO surprise PII egress (grep clean, no `api.qrserver.com`-class leak), bcrypt (tutor 10 / parent 12 rounds), HTTPS/HSTS, ownership assertions, tokenized+revocable share links, consent-gated recording for NEW sessions (frozen `SessionConsentSnapshot` + server gates), Gmail minimal scopes + disconnect deletes token, no analytics/data-sale patterns. **RANKED exposure:** #1 24-mo auto-delete promised/zero-mechanism, #2 "account closed" unimplemented (clock never starts), #3 `deleteStudent` immediate hard-delete vs retention, #4 parent-deletion manual-only, #5 no under-13 age tracking despite VPC claims, #6 educational-use+notify absent, #7 grade-level inventory inaccuracy. **â³ DECISION FOR ANDREW (queued â€” two honest paths, likely BOTH):** **(Path 1, recommended pre-Sarah given real minor data) BUILD mechanisms to match copy** â€” Sonnet design: add `closedAt`/`retentionExpiresAt` (+ decide WHICH "account" the policy means â€” family AH vs tutor-relationship), a nightly retention-sweep cron that auto-creates erasure/purge jobs when `retentionExpiresAt<=now` (reuse existing `processErasureBatch` purge), align `deleteStudent` to soft-delete/queue for claimed minors, add parent-initiated erasure request; `[ret]` log prefix. **(Path 2, interim) SOFTEN copy** where `docs/LEGAL-SYNC.md` allows â€” BUT the COPPA retention language may be UMBRELLA-DERIVED (canonical = mortensenapps.com `coppa-312-10-disclosure`); umbrella sections need ADDITIVE upstream edits, NOT silent truncation on the product facade. The 24-mo schedule was a product-specific 2026-05-31 addition (hybrid per LEGAL-SYNC) â†’ likely editable, but MUST follow the sync protocol (top-of-file sync date + in-UI "Last updated" + classification tables). Interim wording e.g. "retained for the active relationship; deleted upon verified parental request or per our retention practices" â€” NOT "24 months after closure" until the clock exists. **Cheapest immediately-truthful moves (Composer, small):** fix (2) grade-level + (4) signed-URL wording via LEGAL-SYNC protocol (product-specific layers). **Fix files DISJOINT from P0 WWC.** **Tier: Sonnet for the retention-lifecycle design (compliance + data-lifecycle + auth + migration); Composer for the copy-accuracy sync edits.** â†’ mirror to `docs/BACKLOG.md`; cross-ref SEC-ERASURE-TUTOR-GATE + self-service-account-deletion backlog + `docs/LEGAL-SYNC.md`.
- Smokebook doc corrections: `de9b396` (Start-not-starting is tutor-only, not "student view") + `7d9deb7` (item 1 login is email+password â€” **Google OAuth / Google Calendar are NOT wired in the app yet**; the Composer author over-inferred from repo sec-1 `GoogleProvider` mentions â€” watch for other inferred-but-absent features).
- **`ca19c16` â€” dark-mode native `<select>` popup contrast** (recurring "AGAIN" bug): fixed at component level via new shared `src/components/ui/native-select.tsx` + co-located `native-select.css` (`color-scheme: dark` + token-based `<option>` colors, scoped to `[data-slot="native-select"]`, NOT a global hack), following the whiteboard `.mynk-wb-native-select` prior art. Migrated all 3 billing settings selects (Time rounding, Rounding direction, Your timezone). `next build` exit 0, billing jest 2/2. **Follow-up debt:** no Playwright regression added (prior-art helper `assertNativeSelectReadable` in `wb-wave5-polish.spec.ts`); other raw `<select>` surfaces (`NewNoteForm`, `DevToolsClient`, â€¦) could migrate to `NativeSelect`.
- **`bb81cbd` â€” Known-issues & roadmap page refresh** (page was frozen 2026-07-05, stale vs the merge; audit [explore](d6f802fe-6261-4e85-9929-5972b9fcbc89)). Andrew confirmed via AskQuestion: (a) **"Recently improved" restructured into 8 categorized sections** (`recentlyImprovedItems` flat array â†’ `recentlyImprovedSections: {title, items[]}[]`; view renders muted `<h3>` + `BulletList` per section); (b) **removed 3 confirmed-shipped known-issues** (LIVE-status-badge `65f6a93`, sync-pill visibility `65f6a93`, in-person waiting copy `3bf3a7e`); (c) **added "Identity, consent & privacy" bullets** (consent required for minors, guided claim login, data-erasure w/ grace). jest 7/7, `next build` exit 0. **â³ HELD pending Andrew's smoke** (do NOT act until smoke settles each): the two **replay bullets** (BACKLOG **SMOKE-UX-1** reopened â€” if replay scrub/start still broken, REMOVE those bullets + add a known-issue); and add known-issues for **tutor live-board mic-meter**, **Start-not-starting (tutor)**, **self-learner join 404** only if smoke confirms still-broken. Also optional: add a `lastUpdated` field to the content file for future staleness detection.
- **âœ… P0 MERGED into `v1-redesign` 2026-07-09 (`9c36cb1`) â€” RECURRING stroke-bleed regression (Board 2 strokes on Board 3).** Relay gate (`test:wb-sync`) ran on the P0 tip before merge: **jest 860/860; Playwright shards lifecycle 48 / shard2 27 / shard3 22 (+1 flaky retry-pass) / shard4 24 / shard5 21 (incl. new `wb-e3-blank-board-stroke-leak` P0 test GREEN) / shard6 22** = P0 introduces no new sync/bleed regression. **GATE CAVEAT â€” one pre-existing red, NOT caused by P0:** `wb-replay-active-board-tab:95` (replay Board-1 tab stays `aria-selected=false` + `disabled` during scrub) â€” runner classified REAL-FAIL, and I confirmed it **fails identically on `v1-redesign` without P0** â†’ pre-existing (traces to `eb3fb5d` "reds under triage"), same replay-board-tab area Andrew flagged 2026-07-08 (missing PDF icons). Logged in quicklist replay list; fix with the other replay-tab work. **WATCH:** `wb-e2-apply-remote-pdf-stroke-leak` (bleed-adjacent fingerprint-guard spec) was flaky at the gate (failed once, passed on retry) â€” monitor. Original P0 detail below (kept for audit): RECURRING stroke-bleed regression (Board 2 strokes on Board 3). Andrew smoke 2026-07-08 (frustrated â€” regressed MULTIPLE times, "supposed to have tests"). Investigation [explore](9bd88171-5273-4b96-b1f9-0b6f6039c0f5): architecture is ONE shared Excalidraw scene + per-board `pageDataRef` buckets + timing guards â€” NOT strict isolation â†’ bleed is a RACE CLASS, each prior fix (`5601fa3`â†’`ef5fb1a`) closed one window. **Smoking gun: content-fingerprint guard is SKIPPED for blank target boards** (`selectTutorPage` `next.length>0` gate ~`:3407`; `handleExcalidrawChange` ~`:4710`) â†’ switching onto empty Board 3 accepts a stale Board-2 onChange. **Tests missed it:** only cover Board1â†”Board2 + immediate-post-PDF, NOT switch-draw-switch-back on Board 3+; PDF-leak spec false-green in isolation vs marathon. **Andrew's decision (AskQuestion 2026-07-08): `hotfix_then_durable` + orchestrator-decides-priority â†’ I jumped it ahead of replay-trio/shimmer (data bleed = trust-breaker).** HOTFIX scope (Sonnet, additive/extend-don't-rewrite): (1) red-before Playwright relay test for the missing Board3 blank-target scenario; (2) close blank-board fingerprint hole (reject stale foreign onChange onto expected-empty board); (3) bucket-only reads (no live `getSceneElements()` merge/broadcast/snapshot during switch â€” fix fallback ~`:2883`); (4) green-after + existing stroke/isolation specs + jest + 5-axis self-review. Tripwires armed: NO regression loop (stop after 1 genuine attempt), extend-don't-rewrite (stop if restructure needed), dedicated branch + targeted add, never touch relay :3002. **DURABLE FOLLOW-UP (queued, NOT yet dispatched): structural pointer-down bucket-keying (capture pageId at pointer-down, never merge live scene on switch) + student-path parity â€” makes wrong-bucket bleed impossible; Opus-design if it needs architecture.** Orchestrator merges `--no-ff` after Sonnet green + Andrew confirm.
- **â³ IN FLIGHT ([Composer](1fceb50c-5de0-4af6-8103-8b7012dc8c16), background, on tip `7c5c421`) â€” 4-part student-touch WB chrome bundle** â€” âœ… **LANDED** as `90762a9` (tip before P0 branch). Bundle complete. (HintViewer hide + multipoint Done button + Sign-out relocation + styles-sheet clearance). Landscape â‹® fix `e771e4b` already landed so `whiteboard-chrome.css` is free. Targeted `git add` instructed (protect uncommitted docs). Parts detailed below. **Andrew decided (AskQuestion): custom "Done" button + hide noisy hints.**
  - **Part 1 â€” hide Excalidraw `HintViewer` on touch** (absorbs the former standalone eraser-Alt-hint fix). Root cause [explore](9423728d-7ba9-43ea-b424-b2fa7d09063a): `zenModeEnabled` + `.mynk-wb-chrome .App-toolbar{display:none}` hides HintViewer on the **desktop** path (inside toolbar) but the Excalidraw **mobile** path renders it OUTSIDE that container â†’ touch users see "Hold Alt to revertâ€¦" (eraser) and "Click on last point or press Escape or Enter to finish" (multipoint line/arrow) â€” no Alt/Esc/Enter on a phone. Fix: `.mynk-wb-chrome .excalidraw.excalidraw--mobile .HintViewer { display:none !important; }` (scoped to Excalidraw's own mobile class â€” exact leak condition; also covers big touch tablets that get `data-layout="desktop"`; preserves valid hints for real mouse+desktop). Affects tutor-on-touch too.
  - **Part 2 â€” floating "Done" button for multipoint line/arrow on touch.** Root cause [explore](ffb80525-2e30-4b84-9c31-80b75d2d4e0e): touch-finish DOES work in the engine (re-tap within 8px of last point â†’ `actionFinalize`; Excalidraw even ships a native touch Done button) but **our CSS hides Excalidraw's native Done** (`.finalize-button` under the hidden `.layer-ui__wrapper__footer`). Build our OWN chip: visible when `getAppState().multiElement != null && touchLayout`, calls new `triggerFinalize()` helper in `undo-redo.ts` (synthetic Enter/Escape on `.excalidraw` â€” mirrors the existing right-click-finalize handler `WhiteboardWorkspaceClient.tsx:6729-6750` + undo/redo synthetic-key pattern; runs Excalidraw's REAL finalize: binding/loop-close/selection/tool-reset). Detect multipoint via `handleExcalidrawChange` `_appState.multiElement` (currently ignored) or `getAppState()`. NO public `executeAction`/`finalize()` API â€” synthetic key is the sanctioned pattern (see `undo-redo.ts` header). Rejected: re-expose native Done (couples to Excalidraw footer DOM); `renderTopRightUI` (still needs synthetic key). All additive chrome â€” no engine/sync/recorder.
  - **Part 3 â€” relocate student "Sign out" into the top-bar â‹¯ overflow (touch layouts only).** Smoke finding (Andrew 2026-07-08): on small landscape, "Sign out" sits inline BETWEEN the sync pill and the â‹¯ button = accidental-logout trap. Targets [explore](f78ff60d-d4f2-4daa-afaf-0252e7c0066f): `LearnerSignOutButton` (`learner-sign-out`, student-only; `POST /api/auth/learner/logout` â†’ `/students/login`) rendered inline at `WhiteboardWorkspaceClient.tsx:6273` (narrow) + `:6408` (non-narrow, incl. phone-landscape). **Distinct from Exit** (`wb-student-exit`/`handleStudentExit` = leave session, stay logged in â€” leave inline). Fix: append a student-only Sign-out item at the BOTTOM of `renderTopBarOverflowItems()` (before `:5613`), mirror the **"Delete selected"** destructive prior-art (`:5204-5216`: `mynk-wb-popover-sep` + `mynk-wb-menu-item--destructive` + `setOpenMenu(null)`). **Decision (Andrew noted, not blocked): gate inline `!touchLayout` / menu-item `touchLayout` so exactly ONE Sign-out shows and NO reachability gap on wide desktop >1100px (â‹¯ absent there â†’ keep inline).** Update DOM test `StudentLiveWorkspaceClient.dom.test.tsx:378-395` (expects `learner-sign-out` visible WITHOUT opening â‹¯ â€” now menu-gated on touch). Additive chrome, student-only.
  - **Part 4 â€” styles action-sheet bottom clearance (touch).** Smoke finding (Andrew 2026-07-08): the "More styles" row in the styles dropdown/action-sheet on phone is **half-dimmed/clipped** because it sits right at the sheet's bottom edge â€” needs to scroll a smidge more. Likely cause: `WbActionSheet` / props sheet `max-height: min(60dvh, 420px)` (`whiteboard-chrome.css:~1418`) + a bottom fade/scrim overlapping the last row, with insufficient scroll padding to lift it clear. Fix (CSS-only): **diagnose clip-vs-fade**, add `padding-bottom` (scroll room) on the sheet's scroll container so the final row ("More styles"/"Less styles", z-order, Delete) fully clears the bottom edge/fade, and/or trim the fade height. Prove on a touch viewport that the last row is fully visible + tappable. Additive chrome/CSS.
  - **Proof:** real-browser Playwright on touch viewport â€” (a) eraser active â†’ `.HintViewer` not visible; (b) start multipoint line â†’ Done chip visible â†’ tap â†’ element commits + `multiElement` cleared; (c) touch student â†’ Sign out NOT inline, present at bottom of â‹¯ overflow; (d) open styles sheet on phone viewport â†’ last row ("More styles") fully visible/tappable, not clipped/dimmed. Gates: `next build` + `test:wb-jest`. **DISPATCH once fbe87315 lands + pushes.**
- **â³ QUEUED as the NEXT COMBINED "presentation polish" dispatch AFTER the chrome bundle** (both presentation-only, files disjoint from each other AND the bundle; serialize after bundle only because concurrent commits to v1-redesign race the git index + share Playwright runtime). **Part B â€” replay scrubber CLS / layout-jump.** Smoke finding (Andrew 2026-07-08): after dropping the replay scrubber, a below-scrubber status line appears/disappears and **pushes the whole board up then drops it back** (jarring). Root cause [explore](39d06fce-75de-4e2e-baf8-54fed6d875df): `ReplayTimelineScrubber.tsx:110-114` renders "Audio loadingâ€¦ you can press Play once it's ready." **conditionally MOUNTED** (`hasAudio && !audioReady`) as a 2nd block row under `.mynk-wb-replay-timeline__controls` â†’ footer grows/shrinks. Fix (presentation-only): **always render the row at a fixed one-line min-height, toggle visibility/opacity** (fade in place) instead of mount/unmount â€” do this INLINE in `ReplayTimelineScrubber.tsx` (line already uses inline styles) to AVOID `whiteboard-chrome.css` (bundle overlap). `audioReady` semantics untouched. Optional parity: legacy duplicate at `WhiteboardReplay.tsx:1288`. Proof: Playwright/dom asserting footer height stable across the mount condition. Both themes (`.muted` = `var(--text-muted)`). NO overlap with bundle/shimmer files.
- **â³ Part C of the same combined dispatch â€” REMOVE the "Multi-part recording â€” timing may drift at part boundaries" banner.** Andrew (2026-07-08): he'd already asked for this â€” it was a **documented requirement** (`wb-wave5-next-session-bootstrap.md:46` "NO DRIFT WARNING") that WS-G's seamless concat was meant to retire, but the banner still renders at `WhiteboardReplayInFrame.tsx:283-296` gated by `multiSegment`. Every recording is multi-segment pre-reassembly â†’ misleading noise. **Remove** the JSX block + the now-dead `multiSegment` gating var + update tests (`wb-replay-multi-segment-notice` assertions; `wsg-replay-concat.test.ts` "multi-part drift warning input collapses"). Presentation-only; file disjoint from bundle + other polish parts. **NOTE for reliability thread:** it showing at all hints WS-G concat may not always collapse a just-finalized replay to a single canonical blob â€” possible link to Andrew's stroke-bleed/replay observations; do not chase in this dispatch.
- **â³ Part D â€” replay RESUME-FROM-PAUSE on hideâ†’reopen.** Smoke finding (Andrew 2026-07-08): after "Pause and hide replay" then "Replay session", it restarts from the beginning; should resume where paused. Root cause [explore](6a436037-4292-4f2c-ac7a-8d26b94cc41f): persist-once mount is ALREADY correct (replay stays mounted, CSS-hidden) â€” but `handleHideReplay` (`WhiteboardReplayInFrame.tsx:117-130`) + the `isReviewActive`-false effect (`:132-139`) call `seek(0)` TWICE and clear `entryPaintDoneRef`, so the re-entry effect (`:141-164`) re-runs first-open auto-play from 0. **Fix (in `WhiteboardReplayInFrame.tsx`):** on hide `pause()` ONLY (remove both `seek(0)`); do NOT clear `entryPaintDoneRef` on hide; gate entry auto-play to TRUE-first-entry only; on reopen preserve `globalMs` + stay paused. NO changes to `useReplayTimelineController` seek/audio engine. **Reverses an intentional-but-wrong prior decision** â€” flip tests that assert restart-from-0: `WhiteboardReplayInFrame.dom.test.tsx:196-199`, `recording-end-to-end.spec.ts:269`. Risk moderate-low; **WebM entry auto-play race is SMOKE-UX-1's sibling â€” if a re-entry race appears, STOP + escalate (no attempt #3), do not loop.** Overlaps Part C (same file) â†’ same dispatch.
- **â³ Part E â€” replay "Theme" click navigated to `/admin/students/[id]` (INTERMITTENT, root cause probabilistic).** Smoke finding (Andrew 2026-07-08): clicked Theme on the replay screen â†’ navigated to student detail; hit Back â†’ returned; clicked Theme again â†’ no nav. Investigation [explore](328fd72c-abfa-4822-b82e-92f29c42621c): **the Theme button CANNOT navigate** â€” `WbThemeToggle` is `type="button"` + `stopPropagation`, no `<form>`. The `/admin/students/[id]` URL comes ONLY from the `review-back-to-student` native `<a>` (Next `<Link>`) in `ReviewWbTopBar.tsx:33-42`. Most likely: **pre-hydration native-link activation** on first interaction (WS-I class â€” review chrome has NO `clientMounted` gate) or a first-click mis-hit/overlap. Also found a **real duplicate-control bug**: at >1100px TWO `wb-theme-toggle` render (outer `ReviewWbTopBar` trailing + inner `ReplayReadOnlyChromeSlots.tsx:144`) sharing one testid. **Defensive fixes (correct regardless of this glitch, root cause NOT confirmed):** (1) remove duplicate `<WbThemeToggle />` from `ReplayReadOnlyChromeSlots.tsx:144` (ReviewWbTopBar already provides theme for hero+replay); (2) add `clientMounted` hydration guard to the nav links in `ReviewWbTopBar.tsx` (`review-back-to-student`, `review-all-notes`) â€” mirror WS-I pattern (`onClick` preventDefault until mounted). **SKIP** the shared-primitive `WbThemeToggle` change (broadens blast radius to live workspace). Playwright regression: theme click on replay-active review â†’ URL unchanged + theme menu opens. Files: `ReplayReadOnlyChromeSlots.tsx` (replay chrome â€” cohesive) + `ReviewWbTopBar.tsx`. Fold into replay-trio dispatch. If recurs, get Andrew's viewport (maximized?) + which toggle (top-bar vs in-replay). **ALSO folded into Part E â€” end-sessionâ†’student-detail "flash/fast-reload" (Andrew 2026-07-08).** Investigation [explore](8b713812-48e4-4be9-8734-c4929e38a89e): the back control IS a soft Next `<Link>` (no `window.location` on this path) â†’ NOT a hard-nav defect. Flash is COSMETIC: review is a `position:fixed;inset:0;z-index:50` fullscreen overlay that hides admin chrome â†’ on nav-away the overlay tears down + sidebar pops in, and student-detail is `force-dynamic` with NO loading skeleton (+ already `revalidatePath`'d stale by end-session) â†’ cold RSC render pops in = "fast reload" feel. Non-cosmetic edge = same pre-hydration native-`<a>` hard-nav as the theme finding â†’ the Part-E `clientMounted` guard already covers it. **Add to Part E:** (a) `router.prefetch('/admin/students/[id]')` when `SessionReviewMode` mounts (warm the arrival); (b) Playwright assert client-nav (admin layout persists, `navigation.type !== 'reload'`). Do NOT re-add `revalidatePath('/workspace')` (documented regression). Low-severity cosmetic â€” stays queued behind P0.
- **â³ Part G â€” replay top-bar disabled buttons don't dim like sidebar disabled buttons.** Smoke finding (Andrew 2026-07-08): on replay, top-bar Share/Undo/Redo (`DisabledTbBtn`, class `mynk-wb-tb-btn` + native `disabled`) render at full strength while sidebar disabled tools (`mynk-wb-tool-btn:disabled`) are dimmed. Root cause [explore](ec899ec3-f788-4e2d-abc5-6ea46317179f): `whiteboard-chrome.css:234-237` has `.mynk-wb-tool-btn:disabled { opacity:0.5; cursor:not-allowed; }` but there is NO `.mynk-wb-tb-btn:disabled` rule anywhere. **Fix (one-line, composition-preferred):** extend the grouped selector at `whiteboard-chrome.css:234` â†’ `.mynk-wb-tool-btn:disabled, .mynk-wb-tb-btn:disabled { opacity:0.5; cursor:not-allowed; }`. Also fixes live workspace disabled undo/redo (shared classes) â€” intentional consistency, not replay-only. File: `whiteboard-chrome.css` ONLY â€” **serialize AFTER the in-flight bundle** (bundle is writing that monolith CSS). File-disjoint from replay-trio+ TSX (`ReplayTimelineScrubber`/`WhiteboardReplayInFrame`/`ReplayReadOnlyChromeSlots`/`ReviewWbTopBar`) â†’ safe to fold into replay-trio+ dispatch. Presentation-only; both themes (opacity-based, token-agnostic).
- **â³ DISPATCH PLAN (restructured) â€” post-bundle polish splits into TWO focused serial dispatches (not one sprawling part-list):** **(1) REPLAY TRIO+** = Part B (scrubber CLS reserved-slot, `ReplayTimelineScrubber.tsx`) + Part C (remove multi-part banner, `WhiteboardReplayInFrame.tsx`) + Part D (resume-from-pause, `WhiteboardReplayInFrame.tsx` + test flips) + Part E (remove duplicate replay theme toggle + review nav-link hydration guard, `ReplayReadOnlyChromeSlots.tsx` + `ReviewWbTopBar.tsx`) + Part G (top-bar disabled-button dimness, `whiteboard-chrome.css` â€” after bundle) â€” cohesive replay/review chrome, real-browser proof; **(2) NOTES SHIMMER** = Part A (per-field skeletons). Both serialize after the bundle (concurrent commits to v1-redesign race the git index + share Playwright runtime); replay-trio first, then shimmer. Rationale: focus per dispatch; A is fully file-disjoint from B/C/D/E/G.
- **â³ QUEUED (LOW, cosmetic â€” after P0; touches WWC record sites) â€” replay board tabs missing PDF/document icon (parity w/ live tabs).** Andrew smoke 2026-07-08. Investigation [explore](c6618139-5b20-4998-9a8b-49740253207a): replay ALREADY uses the shared `BoardTabStrip` (PDF-icon logic present) â€” icon never fires because replay `pageList` comes from the event log and `page-switch` events record only `pageId`+`title` (no `section`), and `deriveReplayPageListFromLog` (`event-log.ts:388-421`) HARDCODES `section:"board", isPdf:false`. **NOT a JSX fix â€” upstream data.** **Tier A (forward-only):** add optional `section?` to `page-switch` event (`event-log.ts` â€” backward-compat), pass it in `recordPageSwitch` (`useWhiteboardRecorder.ts:837`), pass `pageMeta?.section` at both WWC record sites (`~:3461`, `~:3554`), derive via `enrichPageStripRow`. **Tier B (back-fill EXISTING recorded sessions â€” Andrew's test sessions won't show icon without this):** hydrate `section` from checkpoint `WhiteboardBoardDocumentV1.pageList` (keyed by pageId) in `useReplayTimelineController` + merge in derive. **Recommend A+B together** for full parity. Recorder-path (additive metadata) â€” Composer-tier, but **touches WWC â†’ serialize AFTER P0 bleed fix lands** (WWC overlap). Its OWN small dispatch (not replay-trio+; core change is `event-log.ts`/recorder, not replay chrome JSX). Playwright: assert `.mynk-wb-board-tab__pdf-icon` on replay tabs when a PDF board exists. Low priority.
- **ðŸŸ  QUEUED (pre-Sarah gate â€” Andrew smoke 2026-07-08, flagged with feeling) â€” "View whiteboard" from NOTES lands on the OLD/legacy replay screen; must land on the NEW in-frame replay.** Reopens **SMOKE-BLOCK-2** (was marked resolved TUTOR-ONLY). Investigation [explore](42500a7d-86ce-4898-b741-f3b458d09b3a): **STUDENT/PARENT side = confirmed bug** â€” `ParentShareNoteCard.tsx:150-161` "View whiteboard" â†’ `/s/[token]/whiteboard/[wbsid]` â†’ `src/app/s/[token]/whiteboard/[whiteboardSessionId]/page.tsx:5,196-205` still mounts **legacy `WhiteboardReplay`** (old play btn + `<input type=range>`, no LiveBoardChrome/board-tabs/scrubber). Share side was NEVER migrated when the tutor link was fixed in `c24c1a1` (2026-07-02). **TUTOR side = already on `/workspace`â†’`SessionReviewMode` (NEW shell) via `TutorStudentNoteExpandedBody.tsx:93-100`, BUT lands on the HERO surface (notes+thumbnail), not auto-playing replay â€” must click `wb-review-enter-replay`.** Andrew says tutor ALSO shows old screen â†’ EITHER he means hero-first (wants auto-enter replay) OR a residual legacy landing (verify: does `workspace/page.tsx` ever redirect ended sessions to the legacy `/admin/students/[id]/whiteboard/[wbsid]` page? that legacy `page.tsx` comment claims "workspace redirects here"; also hunt other noteâ†’wb links e.g. student-detail/recent-notes). **FIX (Composer-tier, chrome/routing â€” NOT fragile recorder/sync):** (1) STUDENT â€” remount the `/s/[token]/whiteboard/[wbsid]` page on `WhiteboardReplayInFrame` (`embedded`, read-only, share-scoped top bar back to `/s/${token}`, `ReplayReadOnlyChromeSlots`+`ReplayTimelineScrubber`, public event/snapshot/concat-audio URLs; NO `SessionReviewMode`/`TutorNotesSection` on share; reuse `buildReplayAudioPayload({audience:"share"})`); (2) TUTOR â€” add `?surface=replay` deep-link on the note href â†’ `SessionReviewMode` auto-enters replay on mount; verify & remove any residual workspaceâ†’legacy redirect; (3) OPTIONAL â€” redirect ended-session GETs on the legacy admin standalone page â†’ `/workspace`; deprecate legacy standalone over time. **Test hole:** existing DOM tests assert only HREFS (`TutorStudentNoteExpandedBody.dom.test.tsx`, `ParentShareNoteCard.dom.test.tsx`); NOTHING asserts destination mounts `wb-replay-in-frame` vs `wb-replay`. Add Playwright: note-link â†’ new stack (`wb-replay-in-frame` present, `wb-replay` absent) for BOTH roles. **SEQUENCING: TOUCHES `WhiteboardReplayInFrame.tsx`/`ReplayReadOnlyChromeSlots.tsx`/`ReplayTimelineScrubber.tsx` = SAME FILES as queued replay-trio+ â†’ CANNOT run concurrently with it; also shares the working tree with the in-flight P0 Sonnet.** Options: fold into replay-trio+ (same files, one dispatch) OR run as its own dispatch strictly serialized after replay-trio+. Both after P0 lands. No `WhiteboardWorkspaceClient.tsx` overlap. Reopen SMOKE-BLOCK-2 in `docs/BACKLOG.md` for share/parent parity.
- **â³ QUEUED (UX, Composer â€” Andrew smoke 2026-07-08) â€” unclaimed-student "Create claim link" should be a TOP-LEVEL affordance, not buried at page bottom.** Ties to item-18 "claim flow smoother" + gains urgency if SEC-SHARE-WALL flips wall ON (claiming becomes mandatory to view notes). Investigation [explore](53784050-cc8e-49d7-b958-782a9c8ea49e): the **"Create claim link"** button is in `ClaimInviteSection.tsx:113-115` on the tutor student-detail page (`/admin/students/[id]`), rendered as the **LAST section** ("Parent account", `page.tsx:297-361`) â€” desktop = bottom of scroll; mobile = behind the "More" bottom tab. Unclaimed gate: `claimInvitesEnabled && !student.learnerProfile?.accountHolder && !student.learnerProfileId` (`page.tsx:115-116,307-325`); claimed â†’ `ConnectedParentSection` (disconnect). Mint = `POST /api/students/[id]/claim-invites` â†’ `[clm] action=invited` â†’ clipboard-copy + inline `/claim/<token>` URL. **CHOSEN placement (Option A): dedicated unclaimed-parent claim banner ABOVE the shell, mirroring `StudentErasurePendingBanner` prior-art** (`page.tsx:363-366` renders it above `StudentDetailShell`; uses canonical `Alert`/`AlertTitle`/`AlertDescription` `src/components/ui/alert.tsx`). **Option B (embed in "Ready to teach" strip) REJECTED** â€” that strip is already REPLACED by the consent-required callout for unclaimed students (`StartWhiteboardSession.tsx:42-64`) â†’ would collide. Reuse the existing `ClaimInviteSection` fetch handler (NO new mint path); keep (or slim) the bottom Parent-account section for the connected-parent/link-display state. Add `data-testid` on the promoted CTA. **Tier: Composer, chrome/UX only.** Files: `page.tsx` (+ maybe extract a compact `UnclaimedParentClaimBanner`), `ClaimInviteSection.tsx`; NO `WhiteboardWorkspaceClient.tsx`/replay overlap. **Test hole:** NO existing DOM/Playwright test references `ClaimInviteSection`/"Create claim link" â€” add one asserting unclaimed shows prominent top CTA + claimed does not; `tests/visual/pages.spec.ts` `admin-student-detail` baseline may need refresh. **SEQUENCING: fully file-disjoint from P0 + replay-trio+ + share-replay-routing â†’ could parallelize via isolated worktree, BUT default = after P0 lands + tree reconciled (avoid adding worktree/merge complexity while docs sit uncommitted on the P0 branch).**
- **â³ QUEUED (UX/nav, Composer â€” Andrew smoke 2026-07-08) â€” "Known issues & roadmap" should be a TOP-LEVEL sidebar link, not buried in Settings.** Investigation [explore](716dd7c7-5922-4cbe-a08e-d06cc65e158a). Page today = `/admin/settings/known-issues` (`src/app/admin/settings/known-issues/page.tsx`, view `KnownIssuesRoadmapView.tsx` â† content `src/lib/known-issues-roadmap-content.ts`). Linked from TWO settings spots: settings index row (`admin/settings/page.tsx:30-35` `settingsLinks`) + settings sub-nav rail (`SettingsSubNav.tsx:8-14`). Admin sidebar nav is **text-only, no icons**, and is DUPLICATED across two components that BOTH need the new item: desktop `AdminSidebarNav.tsx:24-45` `buildNavLinks` (+ item render `:93-107`, active-state `isNavActive` `:48-51`) and mobile `AdminNav.tsx:22-42` `buildNavLinks` (hamburger drawer auto-maps). No role gate needed (page only checks `session.user.email`; add alongside Settings for all authed admins). **CHOSEN approach (Composer, veto-able by Andrew): MOVE, not duplicate** â€” (1) **relocate route `/admin/settings/known-issues` â†’ `/admin/known-issues`** (top-level) so it doesn't make **Settings** highlight active via prefix-match (`isNavActive` does `startsWith(\`${href}/\`)`); (2) add `{href:'/admin/known-issues', label:'Known issues & roadmap'}` to BOTH `buildNavLinks` (before Settings); (3) REMOVE the two Settings entries (`settings/page.tsx` row + `SettingsSubNav.tsx`); (4) on the page, drop `sidebar={<SettingsSubNav/>}` + the mobile `â† Settings` eyebrow (it's no longer a settings sub-page); (5) update the nav-wiring test `src/__tests__/admin/known-issues-roadmap.dom.test.tsx:68-74` (asserts settings-index link â†’ point at sidebar) + cosmetic `Settings â†’` comment in the content module. **Alternative (if Andrew prefers keeping the URL): leave route, add sidebar link, special-case `isNavActive` to exclude `known-issues` from Settings-active â€” messier; MOVE is cleaner.** Icon: sidebar has none today â†’ keep text-only (no icon needed; `lucide` `Map` available if we ever add icons). **Tier: Composer, pure presentation/nav â€” NO data/logic.** Files: `AdminSidebarNav.tsx`, `AdminNav.tsx`, `admin/settings/page.tsx`, `SettingsSubNav.tsx`, route move of `known-issues/page.tsx`, the dom test. **NO `WhiteboardWorkspaceClient.tsx`/replay overlap â€” fully disjoint from P0.** Optional bonus (not required): dedupe the two `buildNavLinks` into one shared module. **SEQUENCING: file-disjoint from everything in flight; default = after P0 lands + tree reconciled (avoid worktree/merge churn while docs sit uncommitted on the P0 branch). Trivial â€” good candidate to bundle with the claim-link-banner Composer UX dispatch.**
- **â³ QUEUED (UX, Composer â€” Andrew+wife smoke 2026-07-08) â€” "Manage" button on the PARENT dashboard learner list feels out-of-line with the "Copy" button; move Manage to upper-right of each row.** Investigation [explore](aa6d10c6-eed2-4b1c-8833-dd16a1738354). **Surface = PARENT account dashboard `/account/dashboard` "Your learners" list (`src/app/account/dashboard/page.tsx`), NOT the tutor roster.** Manage = per-learner `<Button asChild><Link href={`/account/children/${profile.id}`}>Manage</Link>` (`:136-143`); Copy = `CopyableLearnerHandle` (`:119-127`) copying the `username@familyid` login handle (NOT a claim/share link). **Root cause of the misalignment:** the `<li>` is `grid grid-cols-[auto_1fr_auto] items-center` (`:98`) â€” Manage sits in a trailing column **vertically centered against the FULL row height** while the name is top and the Copy handle is one line BELOW the name (`mt-2`) inside the middle column â†’ Manage never shares a horizontal band with Copy, worst on mobile. (Also a latent bug: `sm:grid-cols-[auto_1fr_auto_auto]` defines 4 cols but only 3 children â†’ unused col.) **FIX (Composer, presentation-only):** restructure each row into a two-band layout â€” TOP band `flex items-start justify-between gap-3` = avatar+name/badges LEFT, **Manage upper-right** (`shrink-0 rounded-full`, unchanged Button/Link); BOTTOM band = `CopyableLearnerHandle` (or "No loginâ€¦" fallback) full-width under the name. Preserve `href` + `handleCopy`. Mirror the established card-header trailing-action pattern (`account/children/[id]/page.tsx:184-200` `AccountSectionCard actions`). Do NOT hoist Manage into `AccountSectionCard.actions` (that's one action for the whole card; Manage is per-learner). Files: `account/dashboard/page.tsx` (layout only; maybe extract a small row component); `CopyableLearnerHandle.tsx` unchanged. **NO overlap** with P0 WWC, claim-link-banner (tutor `/admin`), double-scrollbar (admin shell), or known-issues-sidebar (admin) â€” this is `/account/**`. Tests hit `/account/dashboard` but assert no Manage/Copy layout â†’ low risk. **SEQUENCING: fully disjoint; trivial Composer â€” bundle with the other small Composer UX tweaks (known-issues sidebar) after P0 + tree reconcile.** â†’ optional mirror to `docs/BACKLOG.md`.
- **ðŸŸ  QUEUED (nav regression, Composer â€” Andrew smoke 2026-07-08) â€” tutor/admin "lost ability to get to the marketing page"; the `?view=home` escape hatch is ORPHANED (zero inbound links).** Investigation [explore](ae2c7198-a5a8-4d9c-b489-9d0966b8c5ea). **`/` IS the marketing landing** (`page.tsx` â†’ `LandingPageContent`); logged-in `/` redirect is **intentional Model B** (`4886712` 2026-06-05): operatorâ†’`/admin`, learnerâ†’`/join`, AHâ†’`/account/dashboard`, UNLESS `?view=home` (`page.tsx:17-38`) which skips all redirects and renders marketing. **REGRESSION: the `?view=home` bypass still exists in `page.tsx` but NOTHING links to it** â€” `eb20040` (2026-06-12, admin wordmarks) then `37cff6b` (2026-07-03, "standardize wordmark nav to canonical '/'", account + marketing header + student shell) repointed every wordmark from `/?view=home` back to `/` â†’ so clicking the wordmark while logged in just bounces to role-home; marketing hero is unreachable via UI (satellite pages `/features`,`/privacy`,`/terms` still render for authed users, only the `/` hero is blocked). Middleware does NOT touch `/` (all logic in `page.tsx`). `37cff6b` conflicted with the ratified Model B contract (`docs/archive/handoff/SMOKE-RUNBOOK-2026-06-05-PM.md:23-24`) â€” a doc/intent flip-flop that today's smoke item 24 didn't catch (item 24 only checks the redirect, not marketing reachability). **FIX (Composer, routing/nav â€” restore Model B):** re-point wordmarks on the app shells to `/?view=home` â€” `AdminNav.tsx`, `AdminSidebarNav.tsx` (`:84-89`), `AccountPageShell.tsx` (`:38-39`, aria already says "View home page"), `MarketingHeader.tsx` (signed-in branch `href={signedIn ? "/?view=home" : "/"}`, `:129-131`); KEEP `href="/"` on `AuthShell` (logged-out), replay chrome, and the inert live-WB wordmark. Extract a `MARKETING_HOME_HREF="/?view=home"` constant to prevent re-drift. **DESIGN NOTE for Andrew (veto point):** this restores the ratified Model B (fast `/` redirect for app-open + explicit wordmarkâ†’marketing escape). Alternative = drop the `/` redirect entirely and show marketing to everyone with a "Go to dashboard" button (regresses the fast-open tutors expect) â€” NOT recommended. If Andrew instead wants a distinct "View site" menu item rather than overloading the wordmark, that's a small variant. **NO P0 WWC overlap.** **Test hole:** item 24 only asserts the redirect; ADD red-before (authed tutor clicks wordmark â†’ lands `/?view=home` + sees hero "Session notes that write themselves") + keep the "authed GET `/` â†’ role-home" assertion green. **SEQUENCING: disjoint; trivial Composer â€” bundle with the known-issues-sidebar + manage-button UX tweaks after P0 + tree reconcile.**
- **â³ QUEUED (layout, Sonnet-tier â€” Andrew smoke 2026-07-08) â€” DOUBLE SCROLLBARS on multiple admin pages; SINGLE architectural root cause.** Andrew wants a shared layout fix, not per-page band-aids. Investigation [explore](160a61da-3bf5-4081-a8e1-d4f5a04c2323): **the admin shell is viewport-locked (`admin/layout.tsx:48` `md:h-dvh md:overflow-hidden` + inner scroll owner `:54` `md:overflow-y-auto`) BUT the document/body is NOT locked (`globals.css:15-17` `html,body{height:100%}`, no `overflow:hidden`) and grows past 100dvh because the global `SiteFooter` renders BELOW the `h-dvh` shell (`layout.tsx:33-36`)** â†’ document scrollbar + inner-column scrollbar = two bars. Introduced by the sticky-sidebar change **`b637370`** (2026-06-12) without coordinating root layout/footer/body overflow. **Student detail is TRIPLE** â€” `StudentDetailShell.tsx:183,256` adds its OWN `md:h-[calc(100dvh-4rem)]`+`overflow-y-auto` nested scroll for scroll-spy tabs (`b57de9e`). **Affected = all `/admin/**` pages under `admin/layout.tsx`** (roster, notes list, settings/billing/known-issues, outbox, dashboard, scheduleâ€¦); student-detail worst (triple). **NOT affected / EXEMPT:** whiteboard workspace (intentional `position:fixed;inset:0` full-bleed overlay â€” leave alone), account routes (`AccountPageShell` `min-h-screen`, no admin lock â€” usually single bar), mobile `<md` (lock inactive â†’ document scroll only), login/auth (`AuthShell`, single column). **FIX (shared, fixes most pages at once â€” do NOT casual-fix):** (1) root layout flex chain â€” make `flex:1` a DIRECT body child + lock `html/body overflow:hidden` for app-shell routes; (2) OMIT `SiteFooter` on `/admin/**` + `/account/**` (keep on marketing/auth/legal) â€” removes the `100dvh+footer` overflow; (3) admin outer `md:h-dvh` â†’ `flex-1 min-h-0 h-full overflow-hidden`, keep `:54` `md:overflow-y-auto` as the SOLE scroll owner (add `data-admin-scroll`); (4) StudentDetailShell â€” remove nested scroll, wire scroll-spy to the admin column. **Tier: Sonnet (cross-cutting layout touching the shell every admin page renders in) OR Composer-execute + Sonnet review â€” REAL-BROWSER Playwright proof MANDATORY (geometry = jsdom blind spot): assert exactly ONE scrollable region per admin page at md/xl; `document.scrollingElement` NOT scrollable on fixed admin pages; regression spot-check login/account/workspace.** Files: `layout.tsx` (GLOBAL â€” affects all routes, careful), `globals.css`, `admin/layout.tsx`, `StudentDetailShell.tsx`, maybe `AccountPageShell.tsx`; NO `WhiteboardWorkspaceClient.tsx`/replay overlap. **OVERLAP: `StudentDetailShell.tsx` is ALSO touched by the claim-link-banner item + `page.tsx` â€” BUNDLE the two student-detail items or serialize; root `layout.tsx` is global (touches everything) â†’ smoke broadly.** Alternative (rejected): revert `b637370` (loses sticky sidebar). After P0; not blocking.
- **ðŸŸ  ACT-NOW (pre-Sarah, Sonnet-tier â€” Andrew smoke 2026-07-08) â€” claim flow lost the "set up child login LATER" option; parent should NOT be forced to create a child-specific login to claim.** Investigation [explore](7f8f95bb-f2a5-4a19-ba2b-92db34595956): **capability EXISTS at data layer** â€” claim creates the parentâ†”student link with `LearnerProfile.accessMode="account_holder_session"` + NO `LearnerCredential` (schema `credential LearnerCredential?` optional; `POST /api/claim/[token]/complete` creates profile w/o credentials; parent views notes via own AH session â€” `/account/children/[id]/notes` + SEC-SHARE-WALL `account_holder` branch â€” independent of any child login). **REGRESSION = UI/policy gating, NOT data-model.** Commit **`7a85d0a`** (2026-07-01, "CC-2 mandatory consent choice at claim setup") wrapped the previously-unconditional **"Set up later"** credential skip in `enforcementEnabled` (`CredentialSetupForm.tsx:303-312`; page passes `enforcementEnabled={!isSelfLearner && !consentComplete}` `setup/page.tsx:63-77,109-189`) â†’ for MINORS the skip is hidden until consent is saved in DB at SSR. **PLUS a client-staleness bug:** `ConsentSetupForm` sets only local `saved` state, NO `router.refresh()` after Save/Decline â†’ credential form keeps `enforcementEnabled={true}` â†’ "Set up later" stays hidden until a MANUAL RELOAD. Combined = looked like the skip was removed. **FIX (Sonnet â€” auth/consent boundary; MUST preserve CC-2 consent-honesty invariant, do NOT revert whole `7a85d0a`):** (1) decouple credential-skip from consent enforcement (deferring child login â‰  skipping consent) â€” always show credential "Set up later" once claim complete; (2) `router.refresh()` (or lift consent-complete state) after consent Save/Decline so skip re-renders without reload; (3) add explicit post-consent "Finish without child login â†’ Dashboard" CTA (not buried below scroll). Files: `setup/page.tsx`, `CredentialSetupForm.tsx`, `ConsentSetupForm.tsx` + update tests that ASSERT the gating (`claim-setup-page.test.tsx` T8, `CredentialSetupForm.dom.test.tsx` enforcement cases) + NEW Playwright `claim-wizard-defer-credentials.spec.ts` (consent done â†’ Set up later â†’ dashboard, DB has NO `LearnerCredential`). **Consent-honesty is a carefully-smoked invariant** (see untracked `docs/handoff/cc1-cc2-consent-gate-smokebook.md` + `consent-honesty-premerge-smoke-index.md`) â†’ Sonnet must keep consent mandatory-choice intact (smoke item 18 PASS). Overlap: NONE with `WhiteboardWorkspaceClient.tsx`(P0)/replay; adjacent-only to SEC-SHARE-WALL (parent access already works); tutor-side `ClaimInviteSection.tsx` (claim-link-banner item) is UPSTREAM (mint), disjoint from this parent-setup fix. **Priority: top pre-Sarah tier alongside SEC-SHARE-WALL decision â€” after P0 (shared tree), Sonnet dispatch.**
  - **â†³ BUNDLED 2nd acceptance criterion (Andrew smoke 2026-07-08 "there is STILL no escape when claiming a student who already has a login") â€” the attach-existing-with-credential DEAD-END.** Investigation [explore](33d38f17-8f42-41dc-b79f-a07c5d7e5574). **DISTINCT symptom, SAME root + SAME files as the "set up later" item above â†’ fix in ONE Sonnet dispatch, two acceptance items.** When a parent claims a learner who **already has a `LearnerCredential`** (interstitial â†’ `attach_existing`), `setup/page.tsx:64,166-179` renders the informational "login is already set up" panel INSTEAD of `CredentialSetupForm` â€” so the form's "Set up later"/dashboard escapes never mount. The ONLY exit ("Go to dashboard â†’", `page.tsx:171-177`) is gated on **`consentComplete`** (SSR, from `7a85d0a`), and because `ConsentSetupForm` does NOT `router.refresh()` after Save/Decline, the SSR `consentComplete` stays `false` â†’ after the parent honestly completes consent they see "âœ“ Preferences saved" but **STILL no dashboard link and NO Back/Cancel anywhere â†’ stuck until a manual browser reload.** Root = the SAME CC-2 over-coupling (dashboard/escape gated on consent-complete SSR) + the SAME missing `router.refresh()`. **Extra fix bits beyond the "set up later" item:** (3b) when `credentialAlreadySet`, show a primary **"Continue â€” login already set up â†’ Dashboard"** CTA that works right after consent Save/Decline without reload; (5) add a `credential_already_exists` 409 guard to the claim setup POST (`api/claim/[token]/setup/route.ts:231-241`) mirroring the dashboard route (`learner-profiles/[id]/credentials/route.ts:91-97`) â€” claim POST currently lacks it; (opt) show a credential/"already has login" indicator on interstitial profile rows (`ClaimInterstitial.tsx:116-144`). Same files as parent item + `api/claim/[token]/setup/route.ts`. Red-before (Playwright): seed parent w/ existing profile+credential â†’ attach_existing â†’ /setup â†’ assert "login is already set up" (not form) â†’ consent Save/Decline â†’ assert dashboard/Continue visible **without reload** â†’ nav succeeds â†’ DB still exactly ONE `LearnerCredential`. Invert the tests that ENCODE the gate (`claim-setup-page.test.tsx` T-new-C/T8). **`router.refresh()` after consent is the single highest-leverage fix â€” it unblocks BOTH this and the "set up later" item.**
- **ðŸŸ  RECURRING (pre-Sarah â€” Andrew smoke 2026-07-08 "STILL not seeing interstitial for logged-in parent; still makes them say they already have an account") â€” `CH-SMOKE-PARENT-INTERSTITIAL`, re-filed 4th time (orig p2b-smoke; 2026-06-30, 07-01, now 07-08).** Investigation [explore](07f4f44b-3a4f-4a1e-b3fb-31805b83e06e). **KEY: the interstitial IS built and correctly wired â€” NOT a missing-feature bug.** `claim/[token]/page.tsx:174-243` reads the **AccountHolder** session (correct realm â€” NOT tutor NextAuth) via `getAccountHolderSessionFromHeaders()`; when it resolves â†’ renders `ClaimInterstitial` ("You're signed in as {email}" + "Not you? Switch account", `ClaimInterstitial.tsx`, `data-testid=claim-interstitial`); when `null` â†’ falls to `ClaimAuthGate` ("Create parent account" / "I already have an account", `ClaimAuthGate.tsx:49-71`). Page is `force-dynamic` (not cached), cookie is `Path=/` (reaches `/claim`) â€” **both ruled out**. **ROOT CAUSE = the `mynk_ah_session` cookie is often NOT ON THE CLAIM REQUEST â†’ session reads `null` â†’ logged-out branch.** Prior fixes (`3d17f50` wired interstitial, `7b9a4fc`+`9de290a` fixed the RSC cookie *read*, `a7f0935` stale-cookie loop-break for `/s/*` only) improved reading WHEN the cookie is present â€” **none fixed cookie DELIVERY.** Ranked delivery gaps: **(1a-ii) PREVIEW DOMAIN SPLIT (RC-A) â€” likely the main one in Andrew's smoke:** claim-invite EMAIL links are built with `getPublicBaseUrl()` (per-deployment `VERCEL_URL`) at `claim-invites/route.ts:89-91`, while Andrew browses/logs-in on the **branch alias** â†’ different host â†’ different cookie jar â†’ AH cookie invisible. (Tutor **copy-link** uses `window.location.origin` = branch alias, so the COPY path is aligned â€” only the EMAIL path is split.) **verify-email already got the RC-A fix (`getRequestBaseUrlSafe`); claim-invite emails did NOT.** (1a-i) **`SameSite=Strict` + opening the link from an email client** = cross-site top-level nav â†’ browser withholds the cookie on FIRST load (same class the verify-done route exists to fix). (1a-iii) tutor-vs-parent confusion (signed into `/admin` NextAuth but no `mynk_ah_session`). (1a-iv) all AH cookies stale â†’ `null`, no stale-clear UX on `/claim` (unlike `/s/*`). Secondary: `setup/page.tsx:22-26` still uses the LEGACY `headers().get("cookie")` read (not migrated to `getAccountHolderSessionFromHeaders()`) â€” same auth cluster, affects post-claim setup not the landing. **âš ï¸ CRITICAL DISAMBIGUATION for Andrew (queued Q): if the repro is "open the claim link from the EMAIL" or "logged in as parent on branch-alias but link points at a different preview host," this is very likely the PREVIEW-ONLY RC-A artifact that WON'T reproduce in production (single stable domain) â€” priority drops hard. If the repro is "tutor copy-link â†’ paste in SAME browser where parent is logged in on the SAME host â†’ still AuthGate," that's a REAL bug.** **FIX (Sonnet, auth boundary â€” do NOT implement till repro pinned):** (1) build claim-invite emails with `getRequestBaseUrlSafe(req)`/branch-alias (mirror verify-email RC-A, `claim-invites/route.ts:89`); (2) SameSite/cross-site handling for email-open (bounce through same-site `/account/login?returnTo=` or a post-mount cookie probe â€” product call); (3) stale-cookie clear parity on `/claim` (mirror `/s/*` `a7f0935`); (4) migrate `setup/page.tsx` session read to `getAccountHolderSessionFromHeaders()`. **File overlap: NONE with P0 WWC; same `/claim/**` cluster as claim-link-buried + claim-"set up later" + claim redesign â†’ coordinate merges.** **Test hole:** existing `claim-wizard-happy-path.spec.ts` pre-seeds the parent cookie (passes, doesn't repro); MISSING = logged-in-parent-after-dashboard same-origin + email-simulated cross-site + preview dual-host. Red-before proposed (`claim-logged-in-interstitial.spec.ts`: AH-session present â†’ interstitial visible + "I already have an account" absent; no-cookie â†’ AuthGate). Already tracked `docs/BACKLOG.md` `CH-SMOKE-PARENT-INTERSTITIAL`.
- **â³ DEFERRED (post-Sarah, PRE-RELEASE â€” Andrew explicit 2026-07-08) â€” rethink the whole CLAIM-SCREEN flow/layout.** "The flow feels clunky; no need to scroll down on such a tiny screen on desktop especially." Catalog (from [explore](7f8f95bb-f2a5-4a19-ba2b-92db34595956), do NOT design yet): multi-step + long scroll on small desktop (success banner + consent card + credential card stacked, `setup/page.tsx:94-191` `max-w-[480px]`); no stepper/progress (reads as one long form, not "claim done + optional extras"); clunky auth path (signupâ†’email-verifyâ†’returnâ†’interstitialâ†’setup, `ClaimAuthGate.tsx`); ties to item-18 "claim flow smoother" + tutor-side claim-link-banner + admin double-scroll. Files for the future redesign: `claim/[token]/page.tsx`, `ClaimAuthGate.tsx`, `ClaimInterstitial.tsx`, `setup/page.tsx` (primary), `ConsentSetupForm.tsx`, `CredentialSetupForm.tsx`, `ClaimInviteSection.tsx`; ref `docs/handoff/v1-design-gap-inventory-2026-06-11.md` (claim setup PARTIAL). Mirror to `docs/BACKLOG.md`. NOT now â€” the act-now credential-skip restore above is the only pre-Sarah claim change.
- **ðŸŸ  ACT-NOW (pre-Sarah, Sonnet-tier, fragile lifecycle/live-A/V â€” Andrew smoke 2026-07-08) â€” tutor CANCEL (pre-start) strands the student in the waiting room; tutor "disappears" but student never navigated away.** Reliability-bar violation (student must ALWAYS exit). Investigation [explore](c8af086b-e4a2-41a3-a124-257d006f818b): tutor Cancel â†’ `handleCancelPendingSession` (`WhiteboardWorkspaceClient.tsx:4111-4135`) calls **`deleteWhiteboardSessionAndDataAction`** (HARD DELETE of the row, `notes-actions.ts:405-467`), NOT `endWhiteboardSession`. Student has NO real-time lifecycle channel â€” only a **3.5s join-timer poll** (`WWC:1340-1453`). On delete the poll gets **404 â†’ mapped to `link_invalid`** (not a clean "canceled"); non-404/network errors are **swallowed** (`catch {}` `:1412-1415,1447-1449`); NO peer-departure trigger during PENDING. Tutor A/V drops INSTANTLY (tutor `window.location.assign` unloads page â†’ mesh presence gone) but lifecycle-nav does not propagate â†’ "tutor disappeared, student stuck." Planned **`cancelPendingSession`â†’`endWhiteboardSession`** (`phase-3-waiting-room-plan-2026-06-16.md`) was **NEVER shipped** â€” WS-F used delete. Contrast: normal End sets `endedAt` â†’ poll returns `{live:false, reason:"session_ended"}` â†’ proper "Session has ended" card (regression-guarded at `wb-session-lifecycle.spec.ts:3111`). **PRODUCT FORK (Andrew to weigh; default = keep-delete):** (1) **KEEP "cancel = delete"** (matches confirm copy "This deletes the session"; pre-start session has no content) + FIX student side: distinguish "was-PENDING-then-404 â†’ `session_canceled`" reason w/ copy "Your tutor canceled this session," add fast poll-refresh when tutor peer drops to 0 during PENDING, stop swallowing poll errors [RECOMMENDED DEFAULT]; OR (2) switch cancel to `endWhiteboardSession` (Phase-3 design) â†’ student gets identical `session_ended` path, BUT canceled sessions PERSIST as empty ENDED records (roster/notes junk). **FIX = Sonnet (lifecycle + live-A/V fragile + reliability rule student-always-exits even if signal missed).** Files: `WhiteboardWorkspaceClient.tsx` (cancel handler + student poll + waiting-room branch), `actions.ts` (if option 2 / new `cancelPendingSession`), `notes-actions.ts` (if keep-delete call site), `join-timer/route.ts` (option 1: map deleteâ†’canceled reason), `join-unavailable-copy.ts` (copy), `wb-session-lifecycle.spec.ts` (ADD two-browser test: tutor cancel while student waiting â†’ student exits + overlay hides â€” current hole covers only tutor-side redirect + student self-leave). **CRITICAL SERIALIZATION: SAME FILE + SAME waiting-room branch as the in-flight P0 stroke-bleed Sonnet AND the SMOKE-PERF-1 finalize fix (also `handleEndSession` in WWC) â†’ this joins the WWC cluster that MUST serialize strictly after P0 (consider one coordinated post-P0 Sonnet session over the WWC lifecycle changes: perf-finalize + cancel-nav, since both touch end/cancel handlers).** Priority: top pre-Sarah tier (live-session reliability).
- **â³ QUEUED (chrome/nav flash, Composer â€” Andrew smoke 2026-07-08 "flash load AGAIN when I started a session") â€” START-session flash = SAME FAMILY as Part E end-flash; bundle them.** Investigation [explore](3e269372-331c-4132-ad93-c3b42210e088): the flash is **path (a)** â€” student-detail "Start whiteboard session" (`StartWhiteboardSession.tsx:152-163`, `type=button`+onClick, NOT a pre-hydration `<form>`/`<a>`) â†’ `createWhiteboardSession` (`actions.ts:275-280`) â†’ server-action **`redirect()`** to the `/workspace` page, which is **`force-dynamic`** (`workspace/page.tsx:28`) with **ZERO `loading.tsx` anywhere in `src/app`** â†’ cold RSC render + admin sidebar visible until the client `.mynk-wb-chrome` (`position:fixed;inset:0;z-index:50`) hydrates over it = pop-in/"fast reload". (In-workspace waiting-room "Start session" = path (b), `activateSessionLive`â†’`setSessionPhase("ACTIVE")`, NO nav/revalidate â†’ should NOT flash; if it does, that's a different bug â€” ask Andrew which surface.) **ROOT = same as Part E** (force-dynamic + no skeleton + fullscreen-overlay timing), OPPOSITE direction (Part E = review overlay teardown â†’ student-detail; START = workspace overlay mount). **FIX (Composer chrome/nav â€” bundle both directions):** (1) add `loading.tsx` skeleton for the workspace segment (full-bleed, matches `.mynk-wb-chrome` bg â€” covers admin shell instantly) + optionally `admin/students/[id]/loading.tsx` (Part E arrival); (2) `router.prefetch` workspace URL on Start-button mount + student-detail URL on `SessionReviewMode` mount (Part E already queues this); (3) OPTIONAL defer `LiveBoardChrome` render until `clientMounted` (opaque same-bg placeholder). **Do NOT** convert `createWhiteboardSession` to `{sessionId}`+`router.push` (redirect() is intentional durability-before-nav) â€” keep redirect + warm with prefetch+skeleton. **Zero `loading.tsx` app-wide â†’ these skeletons also help EVERY force-dynamic nav.** Tier: Composer (skeleton/prefetch/CSS); the optional overlay `clientMounted` gate touches `WhiteboardWorkspaceClient.tsx` â†’ serialize after P0. Core `loading.tsx`+prefetch files (NEW `loading.tsx`, `StartWhiteboardSession.tsx`, `SessionReviewMode.tsx`) are DISJOINT from P0. **Sequencing: bundle with Part E flash work (folded in replay-trio+ or its own "nav-flash skeletons" dispatch); Part E's `ReviewWbTopBar` link hydration-guard stays as-is.** Verification (real browser): `performance.getEntriesByType('navigation')[0].type` + assert admin sidebar not visible on first paint / stable skeleton. COSMETIC (below the act-now reliability + SEC items). Note: stale-oracle #4 spec `tests/smoke/whiteboard-workspace.spec.ts` already guards the redirect+hydration; add a no-flash oracle.
- **ðŸŸ  ACT-NOW (pre-Sarah, Sonnet-tier, session-lifecycle/join-link â€” Andrew smoke 2026-07-08, flagged MAJOR) â€” after tutor CANCELS a session, the next "copy link" for the SAME adult learner hands out the DELETED session's id (`/join/{deletedId}`), not a fresh one â†’ dead link.** Investigation [explore](96f2a270-bd02-471a-9395-1bc65178d962). **Root cause: copy-link is a PURE CLIENT reuse of the workspace route's `whiteboardSessionId` prop â€” it NEVER creates a session and NEVER checks the row is live.** `handleCopyStudentLink` (`WhiteboardWorkspaceClient.tsx:3973-4003`) builds `` `${origin}/join/${whiteboardSessionId}#k=${key}` `` (LEAK LINE `:3993`) with zero server call (token minting retired `a3d9b71`). Student-detail page has NO whiteboard copy-link at all â€” only "Start whiteboard session" + the `/s/` NOTES share. So a stale id can ONLY come from the tutor still being on / returning to the deleted `/workspace/{A}` tab: **cancel uses `window.location.assign(rosterUrl)` (`:4126`, introduced `9450906` to fix soft-push not committing) which LEAVES `/workspace/A` in browser history â†’ Back / BFCache restores the deleted tab (React still has `whiteboardSessionId=A` in props, no server round-trip) â†’ copy re-emits `/join/A`.** Contributing: cancel does NOT `clearEncryptionKeyForSession` (End does, `:4311`); `createWhiteboardSession` omits `revalidatePath('/admin/students/[id]')` (`actions.ts:275-280`) so the open-session list can stay stale. **Severity: DEAD LINK, NOT a data leak** â€” deleted CUID isn't reassigned; learner opening `/join/{deletedId}` â†’ `notFound()` (`join/[sessionId]/page.tsx:90`), never another student's session. Pairs with the cancel-strands-student ACT-NOW above (same handler + student poll). **FIX (Sonnet, do NOT implement yet â€” coordinate):** minimal correct behavior = copy must target a LIVE session: (1) client guard before clipboard â€” check session-ended/existence, disable copy + "Session ended â€” start a new session" if gone; and/or a server `resolveJoinLinkForStudent(studentId)` that finds-`endedAt:null`-or-creates PENDING; (2) cancel nav â†’ `window.location.replace` (drop `/workspace/{id}` from history) + `clearEncryptionKeyForSession` on cancel; (3) add `revalidatePath('/admin/students/[id]')` to `createWhiteboardSession`; (4) BFCache `pageshow` (`event.persisted`) â†’ revalidate session existence on workspace. **File overlap: copy-guard + cancel-history + key-clear all live in `WhiteboardWorkspaceClient.tsx` `handleCopyStudentLink`/`handleCancelPendingSession` â†’ SAME FILE as in-flight P0 bleed AND the cancel-strands-student fix â†’ BUNDLE ONE WWC lifecycle pass, serialize after P0.** `createWhiteboardSession` revalidate is disjoint (`actions.ts`). **Test hole:** no cancelâ†’start-newâ†’copy-freshness and no cancelâ†’Backâ†’copy-blocked test. Red-before (Playwright `wb-session-lifecycle.spec.ts`): create Aâ†’copy(`/join/A`)â†’cancel(row A gone)â†’Start new Bâ†’copy MUST be `/join/B` not `/join/A`; + cancelâ†’`page.goBack()`â†’expect `notFound` OR copy disabled.
- **ðŸŸ¡ NOTES QUALITY thread (Andrew smoke 2026-07-08, real session â€” awaiting scope decision).** Screenshot: Topics OK-ish; **Plan DOUBLED UP** ("double digits next time, double the digits next time, next week"); **Assessment inaccurate** ("strengths in addition/multiplication, wrestling w/ 2Ã—2"). Investigation [explore](92bd9965-e34e-435f-b27c-1e14b55f5fb5) (prompts quoted verbatim in that report). Pipeline = **mapâ†’reduce**, all `gpt-4o-mini`: per-chunk extract (`extract-chunk.ts`, temp 0, no `json_object`) â†’ merge (`notes-worker.ts`, temp 0.3, `json_object`). **Plan-dup root cause:** MAP extracts `followUps` per-chunk independently; REDUCE lists every chunk's follow-ups + system rule "nextSteps covers ALL follow-ups" with **NO dedup/synthesis instruction**. **Assessment root cause:** when extractions exist (â‰ˆalways), REDUCE **never sees raw transcript** â€” only map `topics/questions/corrections`; MAP has **no strengths/assessment field**; REDUCE assessment instructions THINNER than legacy v7 (`ai.ts` â€” has "cluster by topic"+explicit strengths/struggles grounding that reduce lacks). **No Phase 11d eval harness in repo** (fixtures `src/__tests__/fixtures/ambient-transcripts/` absent; deferred post-master per BACKLOG) â†’ prompt tuning = manual smoke only. **Ranked levers:** (1) dedup+synthesis rule in reduce prompt [Composer, prompt-only]; (2) feed reduce full transcript [Sonnet, +tokens]; (3) add strengths field to MAP schema [Sonnet, migration]; (4) port v7 assessment grounding to reduce [Composer]; +temp/model/harness. **DECISION (Andrew AskQuestion 2026-07-08): `prompt_wins`** â€” prompt-only, Composer-tier, no migration: (1) add dedup/synthesis rule to REDUCE system prompt (`notes-worker.ts:73-83`) â€” merge semantically-duplicate follow-ups to ONE phrase, one item per distinct future action, keep completeness but "distinct" not "ALL"; (2) port v7 assessment grounding into REDUCE â€” "cluster by topic", explicit strengths/struggles/mastery, evidence-grounded, no-fabricate (from `ai.ts:86-105`). Bump `REDUCE_PROMPT_VERSION` (`notes-reduce-config.ts:13`) to new dated tag. Update `notes-worker.test.ts` (asserts prompt contains "extractions"/"do not fabricate" â€” keep + add dedup/assessment assertions). Gate: jest (notes-worker + extract-chunk + ai). NO pipeline-shape change (that was option B, NOT chosen), NO map-schema/migration (option 3, not chosen), NO temp/model change (stay prompt-only). Validation = manual smoke on preview (no eval harness). **SEQUENCING: file-disjoint from P0 BUT shares the working tree with the in-flight P0 Sonnet (uncommitted) â†’ CANNOT dispatch concurrently (branch-checkout/index collision). QUEUED to dispatch the moment P0 Sonnet completes.** Own branch `fix/notes-reduce-dedup-assessment` off latest tip â†’ push â†’ Andrew smokes preview â†’ merge `--no-ff`.
- **ðŸ” SHIMMER (Part A) reinforced by smoke.** Andrew on `e771e4b` saw real note text flash in "Topics covered" UNDER the shimmer as it flickered â€” confirms the current full-form-overlay scrim-over-streaming-content problem the queued per-field-skeleton refinement (Part A below) fixes. Shimmer refinement NOT yet shipped anywhere (queued). Not a regression â€” not-yet-done.
- **â³ Part A (its own dispatch, after the replay trio) â€” notes shimmer refinement to per-field skeletons.** Smoke finding (Andrew 2026-07-08): the WS-K notes "generating" shimmer now appears (good) but is a **full-form overlay** over the entire notes form; wants it **inside each text input** â€” per-field blurred/shimmering lines of text, not a form-wide scrim. Targets [explore](0d5c90af-f2b4-4092-9f75-a78f78f6a96e): current = `.tn-notes-generating-wrap::after` gradient overlay in `TutorNotesSection.tsx` (`:423-431`) + `src/styles/tutor-notes-shimmer.css`. 4 textareas: Topics/Assessment/Plan/Links (`NoteField`, `:682-739`). **REUSE the canonical `src/components/ui/skeleton.tsx` primitive** (exists, currently zero usages â€” composition-over-duplication) for per-field lines; keep the existing token-based `@keyframes tn-notes-shimmer` gradient wave (reads "shimmery") + reduced-motion branch; both themes. **Fix:** remove wrapper `::after`; in `NoteField` when `isGenerating`, render 2-3 skeleton lines inside each textarea box (empty fields; streamed content replaces skeleton). **PRESERVE all gating/hooks:** `isActive`, `activeStatusCopy` ("Preparing your notes..."/"Writing notesâ€¦"), `data-note-phase`, testids (`tutor-notes-generating`/`tutor-notes-content`/`tutor-notes-status`), `aria-busy`, readOnly. **Migrate tests intentionally:** `wb-notes-shimmer.spec.ts` `readShimmerOverlayState` â†’ per-field target; `TutorNotesShimmer.dom.test.tsx`; `TutorNotesSection.dom.test.tsx`. Presentation-only â€” NO notes-worker/actions/polling changes. **Confirmed NO overlap** with whiteboard-chrome bundle files. Gates: `next build` + relevant jest. DISPATCH after the chrome bundle lands.
- **BACKLOG (feature, post-Sarah â€” self-service account deletion; Andrew's wife's Q 2026-07-08) â€” parents/students CANNOT self-delete today; add it "at some point."** Investigation [explore](7626a548-ac8d-427c-8f63-193814817cf1). **Today: NO self-service delete anywhere** â€” only paths are operator-only **Compliance Erasure** (`/admin/erasure`, `assertIsAdmin` ADMIN-role only, typed-phrase confirm, 7-day grace, `[ers]` log, tombstoneâ†’blob-purgeâ†’db-scrub via `src/lib/erasure/`), tutor **disconnect** (nulls `Student.learnerProfileId` only, no data deleted), tutor **`deleteStudent`** (hard-deletes tutor's own copy), and the privacy-policy "email us" manual path. Parent self-service was **explicitly deferred** (`session-identity-access-design-2026-05-31.md:674`, `session-experience-arc-continuation-bootstrapper.md:80` "No parent self-service deletion UI"). **CRUCIAL: a full parent self-service design ALREADY EXISTS but is UNBUILT** â€” `docs/handoff/learner-erasure-plan.md` specs `requestLearnerErasureAction(learnerProfileId)` + `requestFamilyErasureAction()` that reuse the SAME erasure engine with a parent principal + stricter (`CRITICAL_ACTION` email-token) confirm; grep confirms neither action exists in `src/`. **Andrew's data-kill question â€” the answer is a FORK (two tracks):** **Track A "delete my child's personal information" (COPPA/GDPR right-to-erasure) = MUST use the same full data-kill** (same `ErasureJob`/tombstone/purge; privacy policy `page.tsx:370-377` grants parents this right) â€” you cannot offer a "delete my data" button that only revokes access. **Track B "close my login / stop using the app" = the LIGHTER path Andrew is intuiting** â€” revoke `AccountHolderSession` + delete/redact AH login credentials, optionally per-tutor unlink, **RETAIN** tutor `Student`/session records per the **terms** (tutor OWNS content, `terms/page.tsx:101-109`) + **24-month-after-closure** COPPA retention (`privacy/page.tsx:358-363`); consent-revocation is already a separate track from deletion (`privacy:389-394`). Track B is architecturally possible but UNBUILT. **So: Andrew's instinct is right for account-CLOSURE, but a true "delete my data" self-serve still needs the heavy erasure engine â€” the risk is shipping a "delete account" button that legally implies erasure while only closing the login.** Identity model (no generic `User`): `AdminUser` (tutor/operator), `AccountHolder` (parent/family login), `LearnerProfile` (child/adult learner, `onDelete: Restrict` to AH), `Student` (tutor-scoped roster, `SetNull` to LP). **Tier: SONNET design episode** (auth boundary `assertOwnsLearnerProfile`, erasure-vs-closure lifecycle, minor/COPPA parent-as-rights-holder, multi-tutor blast radius, legal-copy alignment); execution Composer once spec ratified. **OPEN PRODUCT/LEGAL decisions for Andrew (when scoping):** (1) self-delete = full erasure (Track A) or account-closure (Track B) or BOTH surfaced separately? (2) does tutor session content survive a parent self-delete? (erasure=de-identified-retain; terms=tutor-owns â€” resolve the copy tension); (3) minor-student self-delete require parent approval? (COPPA â†’ parent is rights-holder for under-13); (4) parents get cancel-during-grace or operator-only? (5) per-learner vs family-only in the parent UI? (6) tutor notification on family-initiated erasure? (7) does "account closed" start the 24-mo clock without wiping content? NOT urgent (Andrew "at some point"), post-Sarah. â†’ mirror to `docs/BACKLOG.md`.
- **BACKLOG (future enhancement â€” replay) â€” speaker attribution in replay ("who is speaking").** Andrew 2026-07-08: show WHO is speaking during replay playback. Explicitly flagged as **future**, "possibly done at the same time as video recording" (bundle with the video-record work â€” same replay-participant/A-V surface). Note: the pipeline already has per-voice-stream speaker labels (`streamId`/`speakerId` attached at notes-reduce time, per notes-pipeline [explore](92bd9965-e34e-435f-b27c-1e14b55f5fb5)) â€” replay could surface the same attribution. Not scoped/urgent. â†’ mirror to `docs/BACKLOG.md`.
- **BACKLOG (post-Sarah, pre-release â€” device pickers) â€” camera/mic picker cleanup.** Andrew 2026-07-08: (1) **de-dupe** picker options so the same physical camera/mic isn't listed multiple times (`enumerateDevices()` grouped/virtual/duplicate entries â†’ collapse via `groupId`/label; tutor + student); (2) **phone camera picker = Back/Front only** (Sarah pilot request) â€” use `facingMode` env/user on mobile instead of raw deviceId list; desktop unchanged. Logged to `docs/BACKLOG.md` as `DEVICE-PICKER-DEDUPE` + `DEVICE-PICKER-MOBILE-FACINGMODE` (Â§ v1-redesign durability-wave user-smoke findings). Not pilot-blocking; pre-release polish.
- **ðŸ”´ SMOKE-AUDIO-1 ELEVATED (Andrew + daughter 2026-07-09) â€” first-acquire mic SILENT (not meter-only).** Brio already plugged in, correctly selected, activity indicator dead, **daughter (student) could not hear tutor**. Switch mic away + back â†’ arms. Confirms prior severity discriminator (peer canâ€™t hear = SEVERE capture / reliability-bar). Same workaround class as 2026-07-08. **Also:** student changed phone setting â†’ reconnect incomplete until **tab refresh**. Fix still Sonnet + recorder/live-A/V gates; coordinate with AUDIO-3 shared acquire util. See `docs/BACKLOG.md` **SMOKE-AUDIO-1**.
- **WATCH (unconfirmed, fragile â€” recorder/live-A/V mute) â€” SMOKE-AUDIO-2: possible tutor phantom self-unmute.** Smoke finding (Andrew 2026-07-08): Bug-A mute fix is working (mute carried waiting-roomâ†’session â€” CONFIRMED good), BUT Andrew "could swear I unmuted myself more than once last session." Unconfirmed / single fuzzy recollection â†’ NO dispatch yet, just a logged suspect. Distinct failure mode from Bug A: this is the **unmute path**, not mute-apply. Suspect area (from [explore](ab478625-254e-4410-90f7-f89712424e91)): `isMicMuted` transiently goes false when `externalAudioStream` wires (`useLiveAV.ts:1811-1815`) â€” believed guarded by the "only push mute=true" reconcile effect (`WhiteboardWorkspaceClient.tsx:~2602`), but a residual gap here would produce a phantom unmute. **If it recurs, capture the TRIGGER MOMENT (observable): student join / live-A/V connect / reconnect** â€” those touch the publish stream. Only escalate to a fix (Sonnet, recorder gates) once reproduced with a trigger.
- **ðŸŸ  SMOKE-AUDIO-3 (pre-Sarah, Sonnet-tier, fragile live-A/V â€” Andrew smoke 2026-07-08) â€” STUDENT gets the WRONG mic after leaving a canceled session and joining the NEXT one.** Investigation [explore](2bf32692-e866-401c-973c-1c384d2fa811). Student mic persistence is **learner-scoped, NOT session-scoped** (`tn-mic-device-id:${learnerProfileId}`, `storage.ts:106-118`, introduced `3a8572f` WS-E) â†’ leaving session A + joining session B **reuses A's stored device by design**. **Primary root cause: slot-before-stored acquire RACE in `useLiveAV.requestMic` (`useLiveAV.ts:1131-1157`)** â€” when the enumerate list is non-empty it acquires **`pickedMicSlotRef` (default 0)** FIRST, applying the learner-stored `{deviceId:{exact}}` only in the fallback branch (`:1160-1203`); the slotâ†’stored reconcile effect (`:1710-1722`) is async and can lose the race â†’ slot-0 mic gets acquired AND **persisted** (`:1221-1226`) for the next session. Contributing: (b) NO validation that stored deviceId still exists in fresh `enumerateDevices()` before `{exact}`; student path has **no WS-H `attemptRelaxedMicAcquire`** (that hardening `c26f7ce` was **tutor-`useAudioRecorder`-only** â€” student `useLiveAV` never got it); (d) touch `requestMicAndCam` (`:1469-1473`) ignores stored device on the bundled acquire and overwrites storage; student leave (`handleStudentExit`, `WWC:2085-2097`) does NOT stop tracks or bump generation. **NOT a WS-H regression** â€” student cross-session carry-over is `3a8572f`. **Severity: (c) wrong-but-WORKING device** (most likely) â€” WebRTC sends from the unintended mic (built-in vs headset); student can self-correct via waiting-room picker; NOT a silent/dead capture. Under `tutor_only` consent the student mic isn't in the tutor's recording mix anyway â€” live-A/V-to-tutor quality only. **Same FAMILY as SMOKE-AUDIO-1** (stored-device `{exact}` acquire) but **different surface** (student `useLiveAV` vs tutor `useAudioRecorder`) â†’ shared fix = extract relaxed-acquire+enumerate-validation into `src/lib/av/` and use in both. **FIX (Sonnet, do NOT implement yet):** resolve+validate stored learner device BEFORE slot-0 acquire (or skip slot path when a stored id exists); port `attemptRelaxedMicAcquire`; apply stored device in `requestMicAndCam` too; consider dropping the student's redundant `useAudioRecorder` auto-acquire (double-GUM, pollutes GLOBAL tutor key). Files: `useLiveAV.ts` (primary), `storage.ts`, new `src/lib/av/*acquire*`, `useLiveAvCoordinator.ts`. **Overlap: DISJOINT from in-flight P0 `WhiteboardWorkspaceClient.tsx` (canvas/sync); coordinate util-extraction with SMOKE-AUDIO-1 tutor fix.** **Test hole:** existing `wb-student-mic-persistence.spec.ts` is SAME-session + seeds localStorage manually (doesn't prove acquire path); missing = cross-`sessionId` red-before (pick mic B in A â†’ leave â†’ join NEW session B â†’ assert active track deviceId === mic B, not slot-0) + a race test (stored micB + reconcile-not-yet-run â†’ assert acquire targets micB). â†’ mirror to `docs/BACKLOG.md`.
- **BACKLOG (fragile, recorder-lifecycle) â€” SMOKE-AUDIO-1: first-acquire mic activity indicator dead until device switch-and-back.** Smoke finding (Andrew 2026-07-08): on first login as tutor, mic activity indicator showed NO activity; switching mic away+back revived it. He fixed it BEFORE Start â†’ recording almost certainly fine that session; affected surface most likely the **waiting-room overlay meter**. Investigation [explore](ab478625-254e-4410-90f7-f89712424e91) (read `RECORDER-LIFECYCLE.md`+`LIVE-AV.md`): meter + recording share upstream (`mediaStreamSourceâ†’gainNode`) but SEPARATE downstream taps (meter=`analyser` off gainNode; recording=`recordingMuteGainNodeâ†’recordingDest`). **WS-I mute CANNOT zero the meter â†’ this is NOT a Bug-A regression.** Two meter paths: recorder-graph analyser (top bar, active session) vs cloned-publish `createMicLevelMonitor` (waiting-room overlay, separate AudioContext). **Prime suspect (medium conf): WS-H stored-device persistence (`c26f7ce`, `loadStoredDeviceId()`)** hands back a stale/silent track on first `getUserMedia`; device switch forces fresh GUM via `swapLocalMicSource` â†’ restores signal (explains switch-and-back). Secondary: overlay `createMicLevelMonitor` first-acquire race on cloned track. **Severity discriminator for next time (observable, no logs/DB): open the session replay + listen to the START** â€” audio-from-start = display-only meter glitch; audio-missing-until-switch = SEVERE capture bug (reliability-bar) â†’ escalate immediately. **Fix = Sonnet-tier + full recorder gates; NOT tonight** â€” overlaps in-flight BUNDLE (`WhiteboardWorkspaceClient.tsx`) + touches `mic-recorder-audio.ts`/`useAudioRecorder.ts` (fragile). Likely directions: validate first-acquire stream RMS/`track.muted` before committing graph + retry default device if silent; reconcile WS-H stored deviceId against post-permission enumerate; (re)start meter/overlay monitor when stream ready. Prod confirm via `avx=`/`vad=open`/`obx sizeBytes` (note: `aud=` in registry is NOT actually emitted). â†’ mirror to `docs/BACKLOG.md` next docs pass.
- **ðŸŸ  NEXT SONNET ITEM (after P0 bleed â€” SAME FILE) â€” SMOKE-PERF-1 "Finalizing" slow + Andrew's scaling worry.** Logged `docs/BACKLOG.md`. **Scaling verdict [explore](eb0dc72b-1fa1-4117-a63d-0dcf2b925365): does NOT blow up linearly** â€” the length-heavy work (WS-G audio concat, Whisper transcription, notes LLM reduce) is ALREADY backgrounded (`after()`/outbox) and does NOT gate review; audio uploads per-segment DURING the session so End only drains in-flight stragglers (15s cap), not a full re-upload; server txn is O(#segments)â‰ˆduration/150s (negligible). **What actually makes it slow (fixable):** (1) **awaited snapshot up to 8s** blocking review â€” VIOLATES `RECORDER-LIFECYCLE.md` "snapshot never blocks End" (`snapshot-png.ts:248` 8s cap; awaited at `handleEndSession` step 9 BEFORE atomic end step 10) â€” dominates SHORT sessions; (2) server `countEventsInBlobUrl` re-downloads+parses events blob 2-3Ã— (`assemble-persisted-state.ts:262`) â€” O(events bytes), avoidable (trust client upload); (3) Vercel PREVIEW cold starts (won't bite prod). **Fix (Sonnet + recorder gates):** flip `onSessionEnded()`â†’review the moment durability boundary met (events+audio+`endedAt`), run snapshot in background/`after()`; skip redundant server events re-fetch when client just uploaded. â†’ perceived finalize ~O(1). **End handler is in `WhiteboardWorkspaceClient.tsx` (~:4079-4273) = SAME FILE as in-flight P0 bleed fix â†’ MUST serialize after P0 (coordinated handler change, not drive-by).** Priority: above cosmetic polish, below P0 (data-integrity first).
- **BACKLOG (low, review-later) â€” laser pointer color asymmetry** re-confirmed on hardware (student=red, tutor=blue). Already tracked as **WB-LASER-ICON-CONTRAST** in `docs/BACKLOG.md` (updated with 2026-07-08 re-confirm + role-deterministic-color product question). Not blocking.
- **âœ… LANDED [`e771e4b`](https://github.com/Arangarx/tutoring-notes/commit/e771e4b) (pushed v1-redesign, preview rebuilding) â€” student phone-landscape â‹® overflow buried.** Fix: rail `padding-bottom` clearance restored (re-applies c88ba36 that 64108cf reverted) + page-strip confined to `grid-column: 2/-1` (stops painting over the 44px rail) + rail `z-index:45` + thin theme-token scrollbar + bottom fade. New `@wb-chrome` Playwright oracle (student 844Ã—390, More in viewport, no forced scroll; red-before captured at 844Ã—360). Gates: `next build` exit 0, `test:wb-jest` 859 passed. Did NOT touch uncommitted docs. ([Composer](fbe87315-419f-401e-bd4b-7e4cd42c9287)) REAL bug (chrome/CSS only). Root-caused via [explore](4aa003db-90ab-4d2c-bcc6-b3b0a01dd838): in `phone-landscape` the tool strip is a vertical left rail; the 7th button (â‹® "More" â†’ `wb-more-sheet`: Text / z-order / Delete / Hand) is **in DOM but visually buried** behind the page-strip bottom overlap with **hidden scrollbar** â†’ reads as missing. Portrait (`narrow` horizontal bar) unaffected. **Affects tutor too**, not student-only. Prior fix `c88ba36` (bottom padding to clear strip) was **reverted by `64108cf`**. Fix = restore rail bottom clearance / stop page-strip painting over the 44px rail column / add visible scroll affordance, in `whiteboard-chrome.css` (`.mynk-wb-chrome[data-layout="phone-landscape"]` rules ~1795-1820, ~1888-1898); NO engine/render/breakpoint changes. Proof = **new real-browser Playwright test** at 844Ã—390 student touch asserting â‹® `toBeInViewport()` + clickable WITHOUT forced scroll (existing tutor item-21 test uses `scrollIntoViewIfNeeded` which would MASK this â€” geometry = jsdom blind spot). Gates: `next build` + `test:wb-jest`. Lands on v1-redesign + push (preview rebuild for re-smoke).
- **NON-REGRESSION (no code change) â€” student whiteboard "styles taken away" alarm RESOLVED.** Andrew (phone, student view, pencil active) saw only color + stroke-width and thought styles/indicators were deleted. Root-caused via [explore](ff03074b-63dd-4676-bd02-b17a69c3351e): **nothing removed, no student-slimming, tutor==student identical.** The roughness/sharpness indicator chips are hidden *for pencil only* by **WS-R** (`8e23324`, intentional â€” those knobs are no-ops for freehand ink; chips return on shape tools). Andrew confirmed **"leave it"** â€” the dynamic-to-tool behavior is correct, just caught him off guard (same affordance on tutor). **One genuine minor mobile-UX regression flagged but NOT actioned (Andrew chose leave):** commit `3814234` **reverted** the touch-sheet auto-expand of "More styles" (which Andrew had *positively smoked* at `17dc8d4` on 2026-06-19). "More styles" button still exists (`WbStrokePropsPanel.tsx:216`) â€” just an extra tap + can sit below-fold on small phones. **Optional backlog debt (chrome-only, low-risk):** restore the `touchLayout && openMenu==="props"` â†’ `setMoreStylesOpen(true)` effect if phone users report can't-find-extra-styles; add mobile-viewport Playwright coverage for the props sheet (current `wb-roughness-style.spec.ts` is desktop-1280 only).
- **ðŸ“ FINDING (Andrew smoke 2026-07-08) â€” "when I saved notes it didn't navigate away" = BY DESIGN, maps to existing `CH-SMOKE-SAVE-TO-NOTES-NAV` (P3 UX; re-confirmed by Andrew this smoke).** Investigation [explore](763c4cfe-b81e-44a7-ac6e-b44a748d1421): in-shell `SessionReviewMode` always passes `onSaved` â†’ `TutorNotesSection.handleSave` (`:297-327`) takes the **stay-in-shell** branch (inline chip `wb-save-note-confirmation` "âœ“ Saved â€” visible to parent" + top-bar `wb-review-notes-saved` "âœ“ Notes saved"), NOT the standalone-only `router.push('/admin/students/[id]/notes')` branch. `saveSessionNotesAction` (`notes-actions.ts:281-376`) sets `SessionNote.status=READY` + links `whiteboardSession.noteId` + `revalidatePath` (NO server redirect). Not silent-fail: `{ok:false}` â†’ inline `role="alert"`. **CROSS-LINK to item-17 share FAIL:** a completed Save â†’ READY â†’ share-visible (`/s/[token]` filters `status:{not:"DRAFT"}`). If Save never completed, no READY note â†’ empty share. So item-17 is likely DOWNSTREAM of save/nav (verify in Andrew's repro whether the "âœ“ Saved" chip appeared). **Fix = Composer, `TutorNotesSection.tsx`/`SessionReviewMode.tsx` â€” DISJOINT from notes-prompt (`notes-worker.ts`) + P0 (`WhiteboardWorkspaceClient.tsx`).** Destination is a product choice â†’ queued to Andrew (below #5). Playwright: click `wb-save-note` in relay Endâ†’review â†’ assert URL change + share shows note after save (closes item-17 loop). Not started â€” awaiting Andrew's destination pick.
- **ðŸ““ SMOKEBOOK INLINE TRIAGE (Andrew 2026-07-08, notes written directly under items in [`v1-redesign-durability-wave-usersmoke-2026-07-08.md`](v1-redesign-durability-wave-usersmoke-2026-07-08.md) â€” smokebook DONE/unattended).** NEW backlog items to mirror to `docs/BACKLOG.md`: (a) **item 18 claim flow "technically pass but needs to be smoother"** â€” UX polish on the guided-claim login (not blocking); (b) **item 21 billing** â€” (i) billable time not clearly labeled/surfaced (suspect: end-session review "5 min" is unlabeled) â†’ add explicit "Billable time" label; (ii) **default rounding should be "up" not "nearest"** (small config default flip â€” confirm w/ Andrew before shipping, see #4 below).
- **â“ FOR ANDREW WHEN BACK (unattended queue 2026-07-08) â€” answer/confirm; do NOT block other work on these:**
  1. **Item 3 FAIL repro** ("Start session / workspace mounts"): what exactly failed? self-learner vs consented-minor? resume vs fresh? waiting-room shown or instant-jump? error text? (Later session items passed â†’ it's a sub-case, need the discriminator.)
  2. **Item 17 share-link FAIL repro:** (a) did the link start with `/s/` (parent share from student detail) or `/join/` (workspace Share button)? (b) had you clicked **Save to notes** (â†’ READY) on the review screen first â€” and did the "âœ“ Saved" chip appear? (c) was the session ended? (d) logged-out or as entitled parent? (e) what did you SEE (blank 404 / empty "no notes" / login gate / error)? Repro checklist in [explore](40b5943e-a37f-44e6-aabf-b841e15fdf21). NOTE: likely downstream of the save-nav finding above.
  3. **Item 24 marketing/legal PARTIAL:** which surface partially failed (landing `/`, `/privacy`, `/terms`, or logged-in `/` redirect)? No note said what.
  4. **Billing (item 21):** confirm flip default rounding **nearestâ†’up**? And where/how prominent should billable time surface (end-session review? student session list?).
  5. **Save-to-notes nav destination** (`CH-SMOKE-SAVE-TO-NOTES-NAV`): you want in-shell Save to navigate away â€” WHERE? student detail `/admin/students/[id]`, notes list `/admin/students/[id]/notes` (matches standalone path), or stay + prominent "Go to student" CTA? (Once you pick, small Composer fix.) Also: did you SEE the "âœ“ Saved" chip? (yes â†’ save worked â†’ item-17 is wrong-link/session-state, not save failure).

---

**âœ… 4 STALE-ORACLE SPECS REALIGNED (2026-07-07 ~19:24, [Composer](c02885e8-e2b7-42ca-96bb-e18c55a80ff9), `6dce3b5`, test-only/zero `src/`, each green isolated under `wb-regression`).** #1 chrome-interactions:528 â†’ activate **Rectangle** (not pencil) before Sharp-chip hover assert. #2 notes-shimmer â†’ `waitForNotesGeneratingUiWithShimmer()` asserts `"Preparing your notes..."`/`"Writing notesâ€¦"` + shimmer atomically inside active window (WS-K fast-finalize race fixed). #3 mic-persistence â†’ `openStudentMicPicker` uses waiting-overlay (PENDING) / `wb-student-overflow-av-pickers` (ACTIVE) + **narrow 1000Ã—800 student viewport** (overflow â‹¯ hidden >1100px). #4 whiteboard-workspace smoke â†’ **React-hydration wait before Start click** + `waitForURL` race â€” session IS created (**correction: `actions.ts` still `redirect()`s to `/workspace`; RW-6 removed the consent modal, NOT the redirect; the real bug was pre-hydration clicks, not a stale redirect oracle or missing session**). No skip-gating added; BLOB gate untouched.

**âž¡ï¸ SOLE REMAINING for merge-gate #1: Bug A** `wb-tutor-recording-mute:70` â€” genuine PRODUCT bug on fragile recorder surface (WS-I pre-start mute, recording-branch gain stays 1 vs 0; root-cause `81813fa3`, branch `f748ef7`). Path: **plan-mode step-back â†’ Sonnet 5-axis â†’ fix**; green spec (isolated + in full relay gate) = merge bar. **Good swap point** â€” start fresh chat, `@`-ref this + this doc, pick up Bug A cold.

---

**âœ… TRIAGE VERDICT (2026-07-07 ~18:40, [explore](1a7cec86-5133-4533-8d27-585241c89b93)) â€” 4 of 5 REAL reds are STALE ORACLES, NO new product regressions. Only Bug A is a genuine product bug.** Each of the 4 is a test that never got realigned after an intentional, already-RESOLVED on-branch product change:
- **#1 `wb-chrome-interactions:528`** â€” STALE ORACLE. WS-R (`8e23324`) intentionally hides the roughness/Sharp chip for **pencil**; test activates pencil then looks for "Sharp" (never mounted). `WbStrokePropsPanel` still renders Sharp for rectangle/ellipse/diamond. **Realign:** activate Rectangle (not pencil) before asserting Sharp hover (mirror `wb-roughness-style.spec.ts:57-79`).
- **#2 `wb-notes-shimmer:209`** â€” STALE ORACLE (+ minor timing). WS-K (`859f695`) changed pending copy `"Waiting for transcriptâ€¦"` â†’ `"Preparing your notes..."`; unit tests already locked new copy. **Realign:** L229 expected copy â†’ `"Preparing your notes..."`; assert inside `recordShortSoloSession` active window (WS-K fast-finalize can race the footer away).
- **#3 `wb-student-mic-persistence:150`** â€” STALE ORACLE. WS-M (`39477285`) set `hideDevicePicker:true` on student top-bar mic; device select moved to `wb-waiting-overlay-device-pickers` (PENDING) / `wb-student-overflow-av-pickers` (ACTIVE). **Realign:** `openStudentMicPicker` targets overflow/overlay picker, not `wb-topbar-mic > audio-device-select`.
- **#4 `smoke/whiteboard-workspace:107`** â€” STALE ORACLE (NOT harness â€” BLOB gate at L112 passes under `wb-regression`). Consent-modal removal (RW-6, `2faecd8` stack) killed the `/workspace` redirect; `eb3fb5d` made the spec run at gate. **Realign per RW-6:** drop `/workspace` redirect oracle, assert session row on student detail (or navigate explicitly). **Caveat:** if realigned test STILL shows no session row, escalate to PRODUCT-REGRESSION on `createWhiteboardSession`.
- **#3(Bug A) `wb-tutor-recording-mute:70`** â€” genuine PRODUCT-REGRESSION (WS-I pre-start mute; recording gain stays 1, root-cause `81813fa3`, branch `f748ef7`). Fragile recorder surface â†’ plan-mode â†’ Sonnet 5-axis path; green spec = merge bar.

**NEXT:** (a) dispatch 4 test-realignments (Composer, test-only, verify each green in isolation; #4 must assert session actually created); (b) Bug A via fragile-surface escalation (plan-mode step-back â†’ Sonnet). Then re-run full relay gate for clean merge #1.

---

**âœ… RELAY GATE NOW TRUSTWORTHY â€” all 3 runner defects fixed; trustworthy classification of the 6 reds done (2026-07-07 ~18:37).** Runner defects all resolved: (1) `--project` arg [`38ce3c3`], (2) blob-merge only-last-shard [`fb3c039`], (3) isolation `-g` grep-arg crash [`956e235`](https://github.com/Arangarx/tutoring-notes/commit/956e235) â€” isolation now selects by **`file:line`** (no regex/shell-split fragility). **Direct fresh-server isolation of the 6 genuine failures ([Composer](4ca66c31-d75f-4f42-9519-a1a1a529b812), ~9min):**
- **1 ENV-FLAKE:** `wb-replay-active-board-tab:95` (passes isolated â€” only fails under marathon exhaustion).
- **5 REAL (deterministic, fail initial+retry):** (#1) `wb-chrome-interactions.spec.ts:528/550` â€” "Sharp" roughness chip button NOT FOUND in `wb-props-panel` (`toHaveClass` unreachable); (#3) **`wb-tutor-recording-mute.spec.ts:70/106` = Bug A** â€” recording-branch gain **1 â‰  0** on mute-before-graph (KNOWN real product gap, sanity-check âœ“); (#4) `wb-notes-shimmer.spec.ts:223/229` â€” `tutor-notes-status` not found / copy mismatch (**got "Preparing your notes..." vs expected "Waiting for transcriptâ€¦"** â€” smells STALE ORACLE); (#5) `wb-student-mic-persistence.spec.ts:213` â€” `audio-device-select` not found in `wb-topbar-mic` (30s timeout); (#6) `smoke/whiteboard-workspace.spec.ts:131` â€” after Start, URL stuck on student page not `/workspace` (needs BLOB â€” possible harness).

**âš ï¸ "REAL" = deterministic, spans PRODUCT-REGRESSION vs STALE-ORACLE vs HARNESS-SETUP** â€” NOT auto product bugs. Failure signatures suggest several are stale oracles from wave-5 UI/notes/chrome work (#1 chip, #4 copy, #5 device-select) + Bug A real + #6 possibly harness. **NEXT:** (a) **TRIAGE the 5 REAL** (explore: per-spec regression-vs-stale-oracle-vs-harness, cite product code + git blame master..HEAD, recommend product-fix vs test-realign); (b) fix accordingly (Bug A = fragile plan-modeâ†’Sonnet path). Shard-6 (audio-upload+apply-remote) stays green. Merge-gate #1 = green once the 5 REAL are resolved (fix or justified test-realign).

---

**ðŸŸ¨ RE-RUN with blob fix (2026-07-07 ~18:23, tip `6c96016`, `terminals/472990.txt`) â€” SUPERSEDED by the trustworthy classification above (isolation grep-arg since fixed `956e235`).** Was: blob-merge FIX CONFIRMED, but a 3rd runner defect (isolation grep-arg) corrupts classification. âœ… **Blob fix works:** merge now extracts ALL 6 shard blobs (`lifecycle`+`shard-2..6`); aggregate found **6 unexpected failures** across all shards (cross-shard blindness gone). âŒ **"REAL-FAIL: 6" is UNTRUSTWORTHY** â€” the isolation pass builds `-g <raw test title>` without regex-escaping or a `--` options-terminator, so titles with `--word`, em-dash `â€”`, or `(...)` crash the isolation subprocess on ARG PARSING (not a real test failure) â†’ false REAL-FAIL. Confirmed â‰¥4 of 6 never ran a test: `wb-chrome-interactions:528` (`unknown option '--active'`), `wb-replay-active-board-tab:95` (`Invalid regex /(2/`), `wb-student-mic-persistence:150` (`non-ascii dash â€”`), `whiteboard-workspace:107` (`Invalid regex /(needs/`). Only `wb-tutor-recording-mute:70` (Bug A) + `wb-notes-shimmer:209` MIGHT have run. **The 6 genuine first-attempt failures (from the trustworthy merged JSON) are:** wb-chrome-interactions:528, wb-replay-active-board-tab:95, wb-tutor-recording-mute:70 (Bug A, known-real), wb-notes-shimmer:209, wb-student-mic-persistence:150, smoke/whiteboard-workspace:107. **REAL vs ENV-FLAKE still UNKNOWN** (strong prior: most are ENV-FLAKE marathon-exhaustion per the full-suite baseline + 22-red triage; Bug A is the one confirmed-real). **NEXT:** (a) FIX isolation grep-arg bug (Composer, test-infra â€” regex-escape title + `--` terminator, or select by file+escaped `--grep`); (b) directly isolate-classify the 6 known specs (fresh server each, correct invocation) â€” faster than another full gate; (c) fix the confirmed-real ones (Bug A fragile path). Shard-6 (audio-upload+apply-remote fixes) = 22 passed, exit 0 â€” stays green.

**ðŸš¨ CRITICAL CORRECTION (2026-07-07, confirming relay run tip `995466e`, `terminals/363549.txt`): the sharded runner's GATE VERDICT ONLY EVER REFLECTED SHARD 6 â€” every "validated" claim below for shards 1â€“5 is HOLLOW.** Root cause: each shard's Playwright run cleans `test-results/` (the outputDir), which wipes the prior shard's blob zip; `MERGE_BLOB_DIR = test-results/wb-shard-blobs` lives *inside* that cleaned dir, so at merge time ONLY `shard-6-report.zip` survives. **Proof:** merged JSON (`test-results/wb-shard-merged.json`) `stats = {expected:22, unexpected:0, flaky:0, skipped:0}`, 8 suites = shard-6 exactly. So `merge-reports` + the isolation/REAL-FAIL classifier have NEVER seen shards 1â€“5. The "F2 DONE/validated", "CLEAN GATE VERDICT / 163 across 6 shards", and "REAL-FAIL: 3" claims only ever classified shard 6 (whose 3 reds â€” audio-upload:86/:101, apply-remote:83 â€” coincidentally were all in shard 6, so they surfaced; everything else was invisible).

**TRUE per-shard results this run (read from raw Playwright stdout summaries, ANSI-stripped):**
- **lifecycle (shard 1): 48 passed, exit 0 âœ“**
- **shard-2: 2 failed** â€” `wb-chrome-interactions.spec.ts:528` (selected-chip `toHaveClass` on hover) + **`wb-e2-pdf-stroke-leak.spec.ts:74`** (canonical PDF-leak spec â€” "board-3 strokes stay on board-3", `toBe` â€” the spec we believed passes 3/3!)
- **shard-3: 1 failed** â€” `wb-replay-active-board-tab.spec.ts:95` (scrub updates active replay board tab, `toHaveAttribute`)
- **shard-4: 1 failed** â€” **`wb-tutor-recording-mute.spec.ts:70` = BUG A (WS-I-PRESTART-MUTE), gain 0 at init**
- **shard-5: 3 failed** â€” `wb-notes-shimmer.spec.ts:209` (overlay `toHaveText`) + `wb-student-mic-persistence.spec.ts:150` (persisted mic pre-selected `toBeVisible`) + `smoke/whiteboard-workspace.spec.ts:107` (`createWhiteboardSession` `toHaveURL`, needs BLOB)
- **shard-6: 22 passed, exit 0 âœ“** â€” **audio-upload:86/:101 (`a12d1da`) + apply-remote:83 (`abfbe9a`) fixes CONFIRMED GREEN**

**â‡’ 9 UNCLASSIFIED reds across shards 2â€“5** (isolation never ran on them). Several likely ENV-FLAKE (blob/transcribe/dev-server-exhaustion noise â€” notes-shimmer "real pipeline", whiteboard-workspace "needs BLOB", possibly mic-persistence/replay/chrome timing), but **Bug A (`wb-tutor-recording-mute:70`) is a KNOWN-REAL branch-only product defect** (see A4 root-cause `81813fa3`) and the **canonical `wb-e2-pdf-stroke-leak:74` failing is notable** (needs isolated confirm â€” may be exhaustion). **Bug A was NEVER "non-repro": it lives in shard 4, which the runner never captured.**

**NEXT (revised):** (a) **âœ… DONE â€” runner blob-preservation FIXED ([Composer](58e6f546-7bf1-43ff-83de-a224293e08b0), [`fb3c039`](https://github.com/Arangarx/tutoring-notes/commit/fb3c039)):** TWO wipe mechanisms found â€” (1) Playwright `createRemoveOutputDirsTask` cleans `test-results/` at each run start â†’ moved `MERGE_BLOB_DIR` to repo-root `wb-shard-blobs/` (sibling, not under test-results); (2) BlobReporter `_prepareOutputFile()` self-cleans its own output dir each run â†’ set `PWTEST_BLOB_DO_NOT_REMOVE=1` on shard env. + one-time clear at run start + `.gitignore`. Minimal 2-shard proof: merged JSON covered BOTH spec files (not just last); `node --check` clean; manifest 163/163. (b) **â³ AWAIT ANDREW GO â€” attended re-run** (`test:wb-sync`, ~35-50min) â†’ real REAL-FAIL/ENV-FLAKE classification for the 9 shard-2â€“5 reds. (c) **Triage the REAL ones** (Bug A already confirmed-real â†’ fragile plan-modeâ†’Sonnet path below; canonical pdf-leak:74 + the others need the isolation classification). NOTE: audio-upload:86/:101 + apply-remote:83 (F3) fixes are independently CONFIRMED green in shard 6 â€” those stay done.

---

**âš ï¸ SUPERSEDED by the correction above â€” retained for audit trail:** ~~F2 relay-shards DONE + VALIDATED; merge-gate verdict trustworthy~~ (verdict only covered shard 6):
- **Run #1** aborted instantly â€” F2a runner gave Playwright spec paths where it expected `--project=wb-regression` (Windows `spawnSync shell:true` arg-concat) â†’ **FIXED [`38ce3c3`](https://github.com/Arangarx/tutoring-notes/commit/38ce3c3)** (combined `--project=wb-regression` + positional filters).
- **Run #2** â€” shards 1â€“5 clean (135 pass/0 fail on fresh per-shard :3100 servers; WS-T #7 lifecycle confirmed green), but surfaced two runner defects: shard-6 collapsed (0 pass â€” the 6th shard re-ran `integration-setup` which timed out under cumulative load â†’ dependency-skipped all 22) and the isolation pass OVER-COLLECTED ("99" â€” `parseFailures` matched every `[wb-regression]` stdout progress line, not just reds). Runaway stopped safely (:3002 untouched).
- **Round-2 runner fixes [`e535cca`](https://github.com/Arangarx/tutoring-notes/commit/e535cca):** isolation now reads merged **blob JSON**, collecting only `projectName=wb-regression && status=unexpected` (no passed/skipped/flaky); shards + isolation run `--no-deps` (setup runs once upfront, not per shard); + 8s inter-shard cooldown + orphaned :3100/:3101 node cleanup.
- **Run #3 (post-fix) â€” CLEAN GATE VERDICT:** jest pre-gate **856/856**; all 163 ran across **6 fully-completing shards** (lifecycle 47+1flaky, shard-2 21, shard-3 21, shard-4 22, shard-5 18, shard-6 19 â€” shard-6 no longer collapses); isolation correctly narrowed to **3 genuine `unexpected` failures (not 99)** â†’ classified **REAL-FAIL: 3, ENV-FLAKE: 0**. Full log: `terminals/889258.txt`.

**ðŸ”´ The 3 REAL-FAILs are GENUINE deterministic failures (NOT exhaustion) â€” confirmed by a fresh/idle-machine isolated re-run (`terminals/193129.txt`, 3 failed / 1 passed):**
1. **`audio-upload.spec.ts:86`** â€” "Upload tab visible + dropzone": the `audio-upload-dropzone` element is PRESENT in the DOM but `hidden` after clicking the Upload tab (8Ã— resolved hidden, 5s timeout). Real, repeatable.
2. **`audio-upload.spec.ts:101`** â€” "transcribe + generate populates note form": the `ai-transcribe-btn` ("Transcribe & generate notes") is **`disabled`** so the click times out (120s). Real, repeatable. **âš ï¸ notes/transcribe surface â€” Ship-to-Sarah-gate-adjacent.**
3. **`wb-e2-apply-remote-pdf-stroke-leak.spec.ts:83`** â€” WS-X fingerprint-guard = **known test-only Bug B** (F3; stale/mistimed oracle â€” spec dies at preconditions, guard proven sound 3/3; see A4 WS-X row).

**Bug A `wb-tutor-recording-mute` did NOT appear in the failure set** (run #2 shard-4 23/23; run #3 shard-4 22 pass) â€” consistent non-repro; mute-before-graph-ready race is timing/environment-sensitive; **re-confirm reproduction before touching that fragile surface.**

**âœ… TRIAGE DONE ([explore](ec500d9d-e4de-475f-b34b-9da8ec6ec28c)) â€” all 3 relay reds are TEST-ONLY; ZERO tutor-facing product regressions on `wb-wave5-polish`** (mirrors the earlier full-suite finding: product clean, harness needed hardening). (1) **`audio-upload:86`** = stale oracle â€” B3 always-mount upload pane is `display:none` until `activeTab==="upload"` (default `"record"`); test clicks tab but doesn't wait for pane visible. Behavior pre-exists on `master`; only *exposed* by harness-gate `eb3fb5d`. Fix = test realignment (wait for `audio-tab-upload-pane` visible / `aria-selected`; scroll Notes section into view). (2) **`audio-upload:101`** = stale oracle / already-tracked **RW-7** (`wb-wave5-execution-queue.md` item (b)) â€” test's upload stub returns legacy Vercel JSON, but harness uses mint+PUT blob flow (`a1cc2bd`/`2278013`) â†’ upload never completes â†’ `pendingAudios` empty â†’ transcribe btn correctly disabled; weak `.or(ai-transcribe-btn)` assertion. Fix = harness-aware stub + assert `toBeEnabled()`. **GUARDRAIL: do NOT enable the transcribe btn without an upload â€” that breaks real gating.** (3) **`apply-remote:83`** = known **test-only Bug B / F3** (WS-X) â€” keep its own careful/attended pass (A4 WS-X row: settle L146 onChange-leak question first, then realign).

**âœ… audio-upload realignment DONE (test-only, [`a12d1da`](https://github.com/Arangarx/tutoring-notes/commit/a12d1da), Composer):** :86 â†’ scroll `#student-section-notes` + `switchToUploadTab()` waits for `audio-tab-upload-pane` visible before dropzone assert; :101 (RW-7) â†’ harness-aware upload stub (`route.continue()` mint+PUT) + assert `pending-segment-list` visible + `ai-transcribe-btn` `toBeEnabled()`. Green: 3 passed, 1 retry-recovered flake on :101 (`switchToUploadTab` first-try scroll-spy timing â€” watch in relay). Product unchanged. Dedup debt logged (BACKLOG: `installControllableUploadStub` now 3Ã— inline â†’ extract).

**F3 STEP-BACK DONE ([explore](50bc5027-340d-4f04-81c9-eede3f5e41a5)) â€” verdict: NO product bug; both reds test-only, BUT realignment touches a WS-X product seam.** L159 (`fingerprintActive===true`) = deterministic **stale oracle** (fingerprint cleared by design on first legit post-switch onChange, WWC L4672-4675; spec checks seconds too late). L146 (`every type==="image"`) = **duplicate** of canonical `wb-e2-pdf-stroke-leak.spec.ts` (passes 3/3) â€” not a real leak. `ef5fb1a` applyRemote guard is sound-by-review, all in `WhiteboardWorkspaceClient.tsx` (NO `src/lib/whiteboard/` change). **CATCH:** the spec's real oracle (`__WBX_INJECT_APPLY_REMOTE__` L205-219) has NEVER executed (spec dies at L159 precondition); current injection fires *after* fingerprint clears â†’ doesn't exercise the guard. Correct realignment = retime injection to fire *inside* the fingerprint window â†’ needs a NEW E2E-gated test seam inside WWC `selectTutorPage`/`releaseGuard` (restore the parked `5d80ea8` pre-arm pattern). That's a product-file seam on the most-fragile surface â†’ per WS-X doctrine NOT a blind Composer edit. **AWAIT ANDREW: pick approach â€” (A rec) Sonnet-authored seam + realignment, attended, red-before/green-after proof (revert WWC L1046 guard â†’ oracle must fail) â€” properly discharges A4; (B) minimal test-only drop L146+L159 (risk: injection oracle fires post-clear â†’ misleading pass or confusing red, leaves guard unproven); (C) quarantine spec for go-to-Sarah cut as tracked post-Sarah follow-up (guard sound-by-review + canonical covers steady-state) â†’ unblocks merge gate now.**

**âœ… F3 DONE â€” approach A ([Sonnet](73feb166-e9e4-4f35-8a3b-414776fbc734), [`abfbe9a`](https://github.com/Arangarx/tutoring-notes/commit/abfbe9a)):** additive E2E-gated `__WBX_ON_GUARD_RELEASE__` seam in WWC `releaseGuard` (27 ins / **0 del** â€” verified purely additive + prod-inert, guard clause L1053 confirmed present in commit); spec retimed to inject *inside* the fingerprint window; dropped L141-159 stale/duplicate asserts; oracle `toPass({10_000})`. **Red-before/green-after PROVEN** (guard intact â†’ 2 passed; revert `!has(targetId)` â†’ board-3 stroke leaks, fail both attempts) â†’ `ef5fb1a` guard now test-proven, discharges A4 WS-X.

**âœ… ALL 3 RELAY REDS RESOLVED â€” all test-only realignments, ZERO product regressions** (audio-upload:86 + :101 `a12d1da`; apply-remote:83 `abfbe9a`). Merge-gate #1 is ready for a confirming clean run.

**NEXT:** (a) **Confirming attended relay run** (`test:wb-sync`, full 6-shard, ~50min) â€” should now go fully green; watch the audio-upload :101 retry-covered flake. This is the orchestrator's attended job (needs Andrew's go â€” 50min + live-stack monitoring). (b) **Bug A** `wb-tutor-recording-mute` (WS-I-PRESTART-MUTE, BACKLOG reliability-gap â€” REAL branch-only product defect, must fix before WS-Iâ†’master) â€” NOTE non-repro in relay runs #2/#3; **re-confirm reproduction first**, then fragile: plan-mode â†’ Sonnet 5-axis â†’ fix; green spec = merge bar. (c) Then **Tranche E** (P2-WB-2 â†’ P3).

| Field | Value |
|---|---|
| **Last action completed** | **âœ… Tranche F / F1 RESOLVED-via-defer (Andrew 2026-07-06, Option A).** Diagnosis-recommended global per-test `afterEach` `TRUNCATE ... CASCADE` on `tutoring_notes_test` **FAILED** (~40 failed suites/run Ã— 3 consecutive `jest --workers=1` runs vs ~1-flake baseline) â€” `40P01` deadlocks + FK / "Engine is not yet connected" races. **Reframed root cause:** TRUNCATE's `AccessExclusiveLock` collides with **in-flight fire-and-forget async DB work that outlives test bodies** (same stragglers behind `Cannot log after tests are done` + `CostEvent_..._fkey` leaks). Failed attempt preserved in `git stash@{0}` on `wb-wave5-polish` (message: "F1 truncate attempt (FAILED...)"). **Salvaged + committed [`647ec35`](https://github.com/Arangarx/tutoring-notes/commit/647ec35):** `transcription-worker.test.ts` â€” `jest.mock("@/lib/recording/extract-chunk")` + `afterEach` microtask flush (stops map-phase async leak past teardown; green in isolation; tsc clean). **Dropped (reverted, stays in stash):** `upload-outbox.test.ts` observation-timing change (5000ms wait budget = Jest default timeout â†’ timed out before diagnostic; T1+S1 markers never appeared). **Heavy jest-isolation DEFERRED** as tracked debt; merge-gate jest stays `--workers=1` + retry (~1-flake = loud stale-red, NOT false-green). **Standing gate convention (6)** added to execution-queue test doctrine (docs `971af78`). **âœ… F2a BUILT + COMMITTED [`13c7522`](https://github.com/Arangarx/tutoring-notes/commit/13c7522):** serial sharded `wb-regression` relay runner (`scripts/wb-relay-shard-run.cjs` â€” fresh dev server per shard, manifest-from-config, union-asserted == `--list`, isolation re-run REAL-FAIL/ENV-FLAKE classifier) + safety port helper (`scripts/free-wb-dev-server-ports.cjs` â€” frozen allowlist [3100,3101], throws on any other incl. CLI, **never :3002**) + `test:wb-sync`â†’sharded (monolithic `test:wb-playwright` kept for bisect) + `playwright.config.ts` (`workers:1` on wb-regression, `:3101` webServer gated on `WB_SKIP_3101_WEBSERVER`). **163 enrollment verified static** (manifest union == `--list`); node --check + tsc clean. Orchestrator-reviewed: port allowlist safe, enrollment preserved. **NOT yet validated with a live relay run (F2b, attended).** Caveat to confirm in F2b: per-shard blob output env-var names (`PLAYWRIGHT_BLOB_OUTPUT_DIR/NAME`) â€” `merge-reports` may need adjustment; the gate VERDICT (shard exit + isolation re-run) does NOT depend on the merged HTML report. |
| **ðŸŸ¨ FULL-SUITE BASELINE result (2026-07-06, tip `c941247`, [run](8dbb2ff3-3ebd-47c8-831a-1af13d6e91d5))** | **NO product bugs / regressions / stale oracles found** beyond those already fixed (reassuring for product quality). **BUT the harness cannot currently run clean end-to-end** â€” a genuine confidence + merge-gate blocker, NOT product rot: (1) **jest `--workers=1` is NOT reliably green** â€” `claim-setup-consent-decline.test.ts` H-1 red on a **fixture email unique-constraint collision** (`createClaimedInviteFixture`), i.e. cross-test DB pollution even single-worker (wider than the previously-documented parallel-only "record not found" race); suite now 288/3083 (grew from the stale 283/3062 claim). (2) **`test:wb-sync` never reached the relay** â€” its wb-jest pre-gate flaked on the SAME fixture-email pattern (`register-audio-segment-action` `seedActiveSession` adminUser email). (3) **`auth.setup.ts` timed out** (signout/`/api/auth/csrf` `Unexpected end of JSON input`) â†’ blocked ALL 163 wb-regression specs â†’ **`wb-session-lifecycle` (our WS-T #7 fix) UNVERIFIED this run**; this is a RECURRENCE of the known harness-health flake (queue Â§ (c)), now gating the entire relay. **Conclusion:** the test INFRA (fixture email uniqueness + no per-test cleanup + flaky auth.setup) â€” not product code â€” is what blocks "trust the suite wholesale" + merge-gate #1. Elevates **Tranche D jest-isolation + auth.setup hardening** to highest-leverage confidence work. |
| **ðŸŸ¥ HARNESS-HARDENING result (2026-07-06, [exec run](2f615c58-260f-4962-aca5-686cb2cacb12)) â€” partial + a NEW headline finding** | **Committed `3cb9a7b`** (shared globally-unique `uniq()` helper `src/__tests__/helpers/unique-test-token.ts` â†’ 28 files; email-collision class GONE) + **`8a381ce`** (auth.setup erasure-admin leg now uses NextAuth **API-credentials** login, not flaky UI-signout â€” deviation from the diagnosed `clearCookies()` which itself hung on `/setup-required`; API-login matches parent/learner legs, verified 3Ã—, auth.setup passes ~7.7s). Both pushed. **TWO open problems, NEITHER caused by these test-only changes:** (1) **jest `--workers=1` still can't hold 2 consecutive greens** â€” email red gone, but a SECOND isolation class remains (~1 flake/run, different DB-integration test each time: upload-outbox, share-audio-proxy, erasure-lifecycle, whiteboard-public-concat-audio, claim-setup â€” consistent with the diagnosis's un-done option (ii) "no per-test DB cleanup"). (2) **ðŸš¨ relay now RUNS and shows 22 HARD wb-regression failures + 14 flaky** (124 pass / 4 skip); `wb-session-lifecycle` itself = 48 pass/1 skip (4 flaky-passed-on-retry, WS-T #7 fix holds). These 22 were **MASKED** for an unknown period because auth.setup aborted all 163 specs â€” so the merge-gate relay has NOT been green. **Provably not from this session:** email fix is jest-only (can't touch Playwright specs); auth.setup passes (124 specs ran). â‡’ 22 reds are **pre-existing-masked and/or environmental** â€” under triage. |
| **22-red triage DONE ([classify run](88c2d0a2-eb37-46ec-a165-c6d20184b47e))** | **~65-75% ENVIRONMENTAL** (dev-server exhaustion over the 20-min serial marathon: log full of `ECONNRESET`, blob/transcribe 500s, ffmpeg-unavailable, `webpack.cache ENOENT`; 10/22 share the SAME `wb-session-review-mode`-never-renders-after-End symptom; smoke-workspace 2 = login `waitForURL` stalls). **NOT a branch-wide functional regression.** But **6 specs have concrete assertion failures** worth isolated confirmation, 2 on fragile surfaces: `wb-end-from-roster` (#9 `endedAt=null` after finalize â€” END-SESSION), `wb-tutor-recording-mute` (#19 gain 1â‰ 0), `wb-review-overlay-3paths` roster-VAD (#17 "No audio" shown when VAD seeded), `wb-wave5-polish` item13 (#20 pill "Solo rehearsal" vs `/live/i` â€” LIKELY STALE ORACLE), `wb-e2-apply-remote-pdf-stroke-leak` (#7 fingerprint invariant false â€” WB SYNC), `audio-upload` dropzone (#1 hidden). |
| **ðŸŸ¥ 6-spec isolated re-run DONE ([run](62b75c67-aa14-492f-a176-5b83c7d513f2)) â€” DEFINITIVE** | Fresh dev server per spec, `--workers=1`. **2 CONFIRMED REAL bugs** (deterministic, fail initial+retry â€” both FRAGILE surfaces, STOP before fixing): **(A) `wb-tutor-recording-mute` #19 â€” CONFIRMED REAL ([root-cause 81813fa3](81813fa3-b908-4363-8a48-600387a70c50)).** Mute BEFORE audio graph ready leaves recording-branch gain=**1** (expected 0) â†’ tutor recorded at full gain when they expected muted. Root cause: WS-I mute contract lives at hook layer (`tutorRecordingMutedRef` + `useAudioRecorder.ts:1136`), but real UI path (`WbTopBarMicControl.tsx:63` awaits `onAcquireMic()` before `onToggleMute()`) delivers mute intent only via an async effect (`WhiteboardWorkspaceClient.tsx:2582`), while mount `acquireMic` finishes during the await and graph init hardcodes gain 1 (`mic-recorder-audio.ts:387`) with ref still false. **Branch-introduced (WS-I `f748ef7`, NOT on master â€” master has no recording mute gate at all â‡’ not Sarah-facing today), but means WS-I is INCOMPLETE for pre-start mute (a real completeness gap before WS-I merges).** Fix (fragile): synchronous `setTutorRecordingMute` on the mute click (not effect-only) and/or reorder toggle-before-acquire; optional `createMicAudioGraph` init param; add workspace-bridge DOM test. Composer step-back plan â†’ Sonnet review if changing acquire ordering; do NOT merge without green `wb-tutor-recording-mute.spec.ts`. **(B) `wb-e2-apply-remote-pdf-stroke-leak` #7 â€” REFINED by root-cause ([83b45842](83b45842-d250-4c37-99fa-ffb4a661a3ad)): NOT a confirmed product bug.** The `fingerprintActive=false` (L159) failure is a **stale/mistimed TEST ORACLE** â€” the fingerprint is cleared by design once PDF import settles, so `false` is correct steady state; product guard (`ef5fb1a`) is logically sound but the spec never reaches its real `__WBX_INJECT_APPLY_REMOTE__` oracle (fix-a unproven, not disproven). **Branch-introduced (spec + fix both in `ef5fb1a`, NOT on master â‡’ not Sarah-facing).** L146 RESOLVED ([fda8a070](fda8a070-3faa-423b-a41e-43fa877d711f)): canonical guard spec `wb-e2-pdf-stroke-leak.spec.ts` passed **3/3 isolated** (fresh server each) â†’ NO real onChange stroke leak; L146 was intermittent test-setup timing. **â‡’ Bug B is ENTIRELY test-only** (guard/apply-path sound). Fix = realign `wb-e2-apply-remote-pdf-stroke-leak.spec.ts` (Composer, test-only: inject during fingerprint window / drop mistimed steady-state L159 check). **1 STALE ORACLE** (test-only): **`wb-wave5-polish` item13 #20** â€” expects `/live/i` but product correctly shows **"Solo rehearsal"** during solo grace (verified vs `lifecycle-machine.ts:627` + its unit test); oracle should assert "Solo rehearsal"/"Recording". **3 ENVIRONMENTAL** (pass in isolation): #1 audio-upload dropzone, #3 review-overlay roster-VAD, #9 wb-end-from-roster `endedAt`. **â‡’ FINAL tally of 22 relay reds: exactly 1 REAL product bug (A, mute-gain â€” branch-only WS-I gap, confirmed [81813fa3]); Bug B = test-only (L159 stale oracle + L146 test-timing, guard proven sound 3/3); stale oracle #20 FIXED (`0118359`); ~18 environmental (marathon exhaustion).** Net: the relay-unblock surfaced ONE genuine product defect (WS-I pre-start mute), and it's not on master. |
| **Next action(s)** | **âœ… F2a DONE (`13c7522`).** **ðŸŽ¯ IMMEDIATE â€” F2b, Andrew approved attended-run 2026-07-07 (do this first in the fresh chat):** run the first live sharded relay validation. Command: `npm run test:wb-sync` (or `node scripts/wb-relay-shard-run.cjs`; run `node scripts/wb-relay-shard-run.cjs --manifest-only` first to eyeball the 6-shard split). **Preconditions:** Docker relay :3002 + Postgres :5432 UP; **NEVER free/kill :3002** (only :3100/:3101). **Watch for:** (a) each shard gets a FRESH :3100 dev server (proves no marathon exhaustion â€” the whole point), (b) isolation re-run classifies REAL-FAIL vs ENV-FLAKE correctly, (c) **blob-output caveat** â€” if `merge-reports` finds no zips, fix the per-shard blob env-var names (`PLAYWRIGHT_BLOB_OUTPUT_DIR/NAME`) in `wb-relay-shard-run.cjs`; the gate VERDICT (shard exit + isolation) is unaffected. **Expected 1 real red:** `wb-tutor-recording-mute` (Bug A, WS-I pre-start mute). **Then F3 (Bug B â€” apply-remote spec realignment; author + redâ†’green IN this same relay session, test-only, Composer)** â†’ then **Bug A** (fragile; plan-mode â†’ Sonnet 5-axis â†’ fix; Andrew GO given; green `wb-tutor-recording-mute.spec.ts` = merge bar) â†’ then **Tranche E** (P2-WB-2 â†’ P3-J1..5). **F1 jest-isolation-class-2:** heavy straggler-elimination pass **DEFERRED** (tracked in BACKLOG + GATE/HARNESS Â§ (f)); correct fix = await/mock all fire-and-forget DB paths across ~45 integration suites, THEN per-suite/per-test cleanup works; medium alt = scoped `afterAll` on aggressive-deleter suites only (`note-and-share.test.ts`, erasure-lifecycle, student-crud cascade deletes). **Merge-gate jest:** `--workers=1` + retry; any RED must be re-run in isolation and classified per standing doctrine rule (6) before merge. **Still owed pre-master (gated):** Bug A WS-I pre-start mute fix (Andrew go; fragile surface), Bug B apply-remote spec realignment (test-only), A4 WS-X relay red/green (deferred), WS-T #9 outbox self-heal (design-first), Tranche C WS-G-A poll. Docker relay :3002 + Postgres :5432 UP. |
| **Open Andrew-confirms** | Known-issues in-app page [`89d8d02`](https://github.com/Arangarx/tutoring-notes/commit/89d8d02) â€” **tone/copy sign-off** + remaining content calls (WS-I/WS-N inclusion, WS-O minor-ness). **RESOLVED 2026-07-05:** WS-G concat-lag â†’ **OMIT** from the Sarah-facing page. **Governing principle (Andrew):** *don't call attention to a transient unless it's a bug we intend to fix* (concat-lag is being fixed by WS-G-A â†’ not a standing issue â†’ omit). **WS-U13-a likely MOOT:** in-person fix `3bf3a7e` starts recording on Start w/o a peer, so in-person no longer sits in `awaiting_first_participant` â†’ the "Waiting for your studentâ€¦" copy WS-U13-a targeted is unreachable for in-person (confirm-and-close, not pick-wording). **WS-P O3 (FYI):** `endingState==="error"` keeps deferring reload; unmount clears â€” flag if disagree. Prior standing: map/reduce wording sign-off; SMOKE-PRIV-2; VERIFY-ACCT-1; **Vercel Skew-Protection dashboard toggle (Andrew action).** |
| **ðŸŸ¡ A4 WS-X relay RED â†’ verdict SPEC/SEAM (~90%), fix (a) UNPROVEN (2026-07-05)** | `wb-e2-apply-remote-pdf-stroke-leak.spec.ts` **RED 0/3** ([run](a1f7ec5b-11c6-440d-bdfc-dc8bb0e7f758)). **Root-cause ([verdict](05e8b160-af6e-46d7-985b-3ceae9d62b14)): shipped fix `ef5fb1a` (fix (a) applyRemote fingerprint-guard) NEITHER proven nor disproven** â€” spec dies at preconditions, never reaches the `__WBX_INJECT_APPLY_REMOTE__` oracle. **(1) Spec mistimed (~95%):** L159 checks `fingerprintActive===true` AFTER import UI settles, but Excalidraw's onChange clears the fingerprint by design (WWC L4678) â†’ `false` is expected steady state. Spec (authored WITH `ef5fb1a`) dropped WIP `5d80ea8`'s pre-armed `releaseGuard`-synchronous injection for post-import `page.evaluate` which can't observe an active fingerprint. Realign: restore synchronous-injection timing / add `__WBX_ON_GUARD_RELEASE__` seam; split steady-state oracle into `wb-e2-pdf-stroke-leak.spec.ts`. **(2) L146 ~60% SEPARATE onChange-path leak** (live board-3 `line` on PDF board pre-injection, NOT tombstone artifact â€” `getSceneElements()` excludes deleted) â€” orthogonal to fix (a); if real = genuine BUG-3 recurrence â†’ needs own instrumented steady-state run. **WS-X = most fragile surface + multi-attempt history â†’ NO blind 3rd fix; both parts = attended + careful (Andrew + Sonnet on any product seam).** **ðŸ…¿ï¸ DEFERRED per Andrew 2026-07-05 to a dedicated attended session / design pass** (order: instrumented steady-state run to settle the L146 onChange-leak question FIRST â†’ then realign the apply-remote spec to actually prove fix (a)). **Â§6 WS-X proof NOT discharged; WS-X NOT proven.** |
| **A2 WS-P fix â€” âœ… DONE (5-axis SHIP)** | Prod-inert PW-gated reload seam in `triggerDeployReload()` (`capture-defer-registry.ts` L47-52) + spec tidy (dropped redundant `Location.prototype.reload` patch) â€” [impl](d4b5c4a3-fa52-4352-8083-18556e782c6a). **A2 Playwright PASSES 2/2** (confirms WS-P reload path genuinely fires â€” seam verdict validated, NOT product bug). tsc clean; 25/25 deploy jest. **Sonnet 5-axis = SHIP, no fixes** ([review](bc6ca5be-4182-432f-a239-ffa4269a4949)): prod-inert (build-time gate; `location.reload()` unconditional), no security surface, test now structurally false-green-proof (flag set by product, not test). **COMMITTED [`4b085db`](https://github.com/Arangarx/tutoring-notes/commit/4b085db). Â§6 A2 proof DISCHARGED.** |
| **WS-T #8/#9 bug context (P1-WB-8)** | **âœ… WS-T #8 FIXED [`a37e9b3`](https://github.com/Arangarx/tutoring-notes/commit/a37e9b3) + PUSHED (2026-07-06).** Option B 3-state honest overlay: (1) `hasAudio` â†’ "â–¶ Replay session"; (2) `!hasAudio && eventCount>0` â†’ CTA + `wb-review-no-audio-note`; (3) nothing â†’ `wb-review-no-recording`. Sonnet 5-axis = SHIP [sa](cfc78841-f623-493b-95b5-86ccc90107c2). jest 8/8; relay 6-pass/1-skip (gate no-VAD stays fixme=WS-T #9). **MINOR follow-up (verify-not-fix):** roster no-VAD state-2 branch possibly test-unreachable â€” reconcile if needed. **âœ… WS-T #9 gate-IDB-crash investigation [sa](2094ba5a-4669-4ace-9d08-0d9353a3c204) DONE (root-cause):** crash **DOMINANTLY test-harness poison** â€” spec helper `countUploadedTutorMicOutbox` (+ `wb-end-from-gate.spec.ts:76-85`) opens `tutoring-notes-upload-outbox` v1 WITHOUT `onupgradeneeded` â†’ empty-v1 DB poisons subsequent product open; intermittency = shared Playwright IDB. **Real product fragility (narrower):** `upload-outbox.ts` lazy schema assumes first opener runs `onupgradeneeded`; gate-only End is first opener (no mount) â†’ any v1-empty outbox DB bricks gate/roster finalize. **Fix (design-first, DEFERRED):** version-bump self-heal in `upload-outbox.ts openDb` (additive, NEVER drop rows â€” Pillar 2) + gate finalize try/catch + de-poison test helpers. Store: `tutoring-notes-upload-outbox`/`rows`/v1. **WS-T #7 (`d9a6c4d`):** 4 pre-existing `wb-session-lifecycle` reds, triage owed before master-cut. |
| **âš ï¸ Relay re-run gotcha (2026-07-05, learned the hard way)** | **NEVER kill/free port :3002** to unblock a Playwright webServer â€” :3002 is the **persistent Docker relay / Docker backend pipe**; killing its process wedges Docker Desktop (needs manual restart) AND takes Postgres down. Correct pattern: **leave :3002 alone**, use `$env:CI='1'` (fresh webServer lifecycle w/ `reuseExistingServer` off) or only free dev-server ports **:3100/:3101** between serial runs. Re-dispatch A2â†’A3â†’A4 with this constraint after Docker is healthy. |
| **Uncommitted / unmerged** | Branch **`wb-wave5-polish`** @ tip [`13c7522`](https://github.com/Arangarx/tutoring-notes/commit/13c7522) (F2a sharded runner), worktree **`tutoring-notes-polishwt`**; tree **CLEAN**, **fully pushed (0 ahead / 0 behind origin)**. **NOT merged** to `v1-redesign`/`master` (Andrew hard stop). Failed F1 truncate attempt preserved in **`git stash@{0}`** (recoverable; not applied). **Merge-gate proofs:** A1 `next build` âœ…, A2 WS-P defer-reload seam âœ…, A3 in-person relay âœ…; **A4 WS-X relay OWED/deferred; full sharded `test:wb-sync` (F2b) OWED on integrated tip.** |

**Autonomy posture (2026-07-05):** **LIMITED** â€” proceed unattended only on safe non-fragile work (jest-isolation, state/docs, pure-jest batches). **Park** anything fragile/gated for Andrew.

**Andrew decisions ledger (2026-07-06):** approved Tranche E â†’ then chose fix+baseline â†’ then harden-now â†’ then root-cause-both-bugs â†’ then swap â†’ **(post-swap) approved Tranche F infra-first:** F1 jest-isolation-2 â†’ F2 relay-shards â†’ F3 Bug B (test-only); **Bug A = GO** with plan-mode design â†’ Sonnet 5-axis â†’ fix (green `wb-tutor-recording-mute.spec.ts` = merge bar); then resume Tranche E (P2-WB-2 â†’ P3). **F1 scope decision (Andrew, Option A):** salvage good test-only fix(es), revert truncate, **DEFER** heavy jest-isolation as tracked debt, keep merge-gate jest at `--workers=1`+retry â†’ **F2 relay-shards** then Tranche E. **(2026-07-07) F2a built+committed+pushed (`13c7522`, orchestrator-reviewed); Andrew approved: run the attended sharded `test:wb-sync` validation NOW (F2b) + swap to a fresh chat.** F2b is conductor-tier Composer/Sonnet (known-loop attended validation, not Opus).

**âœ… F1 RESOLVED-via-defer (2026-07-06, post-swap) â€” global per-test TRUNCATE mechanism FAILED; Andrew chose Option A.** Diagnosis ([b638f945](b638f945-7db5-4ac6-88f0-c3c3a93a3497)) recommended a global `afterEach` `TRUNCATE ... CASCADE` on `tutoring_notes_test`; implementation produced **~40 failed suites/run Ã— 3** consecutive `jest --workers=1` runs (vs ~1-flake baseline) â€” `40P01` deadlocks + FK/"Engine is not yet connected" races. **Reframed root cause:** TRUNCATE's `AccessExclusiveLock` collides with **in-flight fire-and-forget async DB work that outlives test bodies** (same stragglers as `Cannot log after tests are done` + `CostEvent_..._fkey` leaks) â†’ *any* between-test cleanup races them. **Correct fix (deferred):** eliminate stragglers (await/mock all fire-and-forget DB paths across ~45 integration suites), THEN simple per-suite/per-test cleanup works. **Medium alt:** scoped per-suite `afterAll` on aggressive-deleter suites only (`note-and-share.test.ts` unscoped `deleteMany`, erasure-lifecycle, student-crud cascade deletes). Failed attempt in `git stash@{0}` on `wb-wave5-polish` ("F1 truncate attempt (FAILED...)") â€” `reset-test-database.ts` + `jest.setup-db-cleanup.ts` + `jest.config.ts` wiring + `note-and-share.test.ts` cleanup removal (NOT committed). **Salvaged [`647ec35`](https://github.com/Arangarx/tutoring-notes/commit/647ec35):** transcription-worker extract-chunk mock + microtask flush only. **Dropped:** upload-outbox observation-timing (5000ms wait = Jest timeout; T1+S1 never appeared; not shippable). Baseline restored; tree clean @ [`647ec35`](https://github.com/Arangarx/tutoring-notes/commit/647ec35).

**Worktree discipline:** execute on **`tutoring-notes-polishwt`** / **`wb-wave5-polish`**. Default checkout `tutoring-notes` is on `v1-redesign` â€” do not use it for wave-5 work.

**Wave-5 queue (authoritative backlog):** [`wb-wave5-execution-queue.md`](wb-wave5-execution-queue.md). **Andrew's smoke results:** [`go-to-sarah-master-cut-smokebook.md`](go-to-sarah-master-cut-smokebook.md) (do not edit).

---

## Project arc + North Star

Pre-public pilot with one tutor (Sarah). North Star from [`AGENTS.md`](../../AGENTS.md): *"People need to use the app with confidence. Sarah is being patient, but that won't last forever."* Reliability bar: [`../../agenticPipeline/.cursor/rules/reliability-bar.mdc`](../../agenticPipeline/.cursor/rules/reliability-bar.mdc).

**Strategic posture:** Experience-driven wedge â€” WB + reliability = ground floor (GATE); the win = accreting honest tutor-first continuity. [`experience-driven_wedge_ae2776e1.plan.md`](../../../../.cursor/plans/experience-driven_wedge_ae2776e1.plan.md).

**Active execution:** **go-to-Sarah master-cut** on `wb-wave5-polish` â€” durability pillars WS-A..D landed; P1/P2 fix trains largely complete; fragile-fix train done; parked at **merge gate** for Andrew hardware re-smoke + relay proofs. **Ship-to-Sarah gate** governs cut to `v1-redesign â†’ master` (see below).

---

## Branch layering

```
master  â†  v1-redesign  (integration base; Wave 4 merged; held for Sarah gate)
              â†‘
                    â””â”€â”€ wb-wave5-polish @ 8ff2553  (active; worktree tutoring-notes-polishwt)
                    â”œâ”€â”€ wb-av-reachability-detection-fix @ a962171  (isolated; PARKED)
                    â””â”€â”€ wb-wave5-ws-x-wip @ 5d80ea8  (WIP seam preserved; superseded by ef5fb1a on polish)
```

| Branch | Role | Tip |
|---|---|---|
| **`v1-redesign`** | Integration base; not yet merged to `master` | [`bf1a2c3`](https://github.com/Arangarx/tutoring-notes/commit/bf1a2c3) |
| **`wb-wave5-polish`** | **Active** â€” Wave 5 + master-cut plan + Part-2 test buildout | [`8ff2553`](https://github.com/Arangarx/tutoring-notes/commit/8ff2553) (code tip; state-doc [`b357ebb`](https://github.com/Arangarx/tutoring-notes/commit/b357ebb)) |
| **`wb-av-reachability-detection-fix`** | SMOKE-BLOCK-1 reachability; Andrew parked 2026-07-03 | [`a962171`](https://github.com/Arangarx/tutoring-notes/commit/a962171) |

**Merge discipline:** single `merge --no-ff` to `v1-redesign` only after comprehensive both-theme master-cut smoke PASS. No interim merge. Ledger: [`v1-redesign-STATUS.md`](v1-redesign-STATUS.md).

---

## Wave-5 status (reconciled with execution queue)

### Landed on branch (do not re-do)

| Area | Status | Tip / note |
|---|---|---|
| **Durability pillars** | âœ… WS-A (VAD + per-speaker + outbox mid-session register `234c6d7`), WS-B (~1s persist), WS-C (endâ†’review), WS-D (resume-from-backend) | Overnight wave; relay @ `c2ca8f5` workers=4 honest |
| **P1 fix train** | âœ… WS-I, WS-N/N4, WS-L, WS-G (`d20ea9a`), WS-K (`859f695`), WS-W (`610ee90`), WS-P 1/3/4 (`b386ef6`), **WS-P 2** (`9ca410e`â†’`2c7a7bd`, 5-axis'd) | Each fragile item 5-axis reviewed |
| **P2 UX train** | âœ… WS-F, WS-H, WS-J (`1d23fc6`), WS-M, WS-Q copy, WS-R, WS-U-COPY (`dfe1bf4`), WS-U-FRAGILE 2.4+2.5 (`65f6a93`) | P2 train **COMPLETE** |
| **Fragile-fix train** | âœ… In-person audio `3bf3a7e`; WS-X BUG-3 `ef5fb1a` | See Â§ Fragile-fix outcomes |
| **Known-issues page** | âœ… In-app `/admin/settings/known-issues` | `89d8d02`; FOR ANDREW: copy/tone |
| **PART-2 pure-jest tranche** | âœ… P1-J1..J8, P2-J2/J3/J4, RW-B1/B3/B4 | `jest --workers=1` green (283 suites / 3062); parallel-DB flake debt |
| **Blob-gate harness** | âœ… Phase 1+2 (`a1cc2bd`â†’`eb3fb5d`); build-green `2278013` | 5-axis SHIP-WITH-FIXES; SF applied |
| **Master-cut smokebook** | âœ… Authored; CUT-3 filled | PARKED at merge gate |

### Open / next tranche

| Item | Priority | Notes |
|---|---|---|
| **WS-P deliverable 2** | P1 | Version poll + capture-defer registry; WS-P-A tutor-only defer + WS-P-B WWC read-only `useEffect` **acknowledged** â€” implementation queued |
| **WS-G-A** | P1 polish | "Preparing seamless replayâ€¦" poll when concat async; default was ship core + defer UX |
| **WS-A F-1** | Pre-merge SHOULD-FIX | Outbox register-failure attempt cap (~10 lines); own 5-axis before merge |
| **WS-N5** | P1 follow-up | Resume FSM armed stroke window |
| **WS-U-FRAGILE 1.2** | PARKED | Dead Start / SMOKE-BLOCK-1 reachability â€” hardest; peer-mesh surface |
| **WS-U-FRAGILE 1.3 copy** | PARKED | In-person waiting copy mooted by in-person fix (no longer sits in `awaiting_first_participant`) |
| **PART-2 relay/identity** | In flight | P1-WB-1..10, P1-ID-*, RW-B2, P2/P3 batches â€” **serial**, attended + Docker |
| **Jest isolation** | Infra | `--workers=1` gate until dedicated pass; do not rush unattended |
| **PART 3 slim smokebook** | After PART-2 | Human-only surfaces |

**Standing rules:** new teeth specs â†’ enroll in `wb-regression.testMatch` + `@wb-*` tag + `--list` verify. Merge-gate jest â†’ `--workers=1` until isolation pass lands.

---

## Fragile-fix train outcomes (durable)

### In-person audio â€” BACKLOG SMOKE-BLOCK-5 resolved

- **Product call (Andrew):** treat IN_PERSON like solo â€” start tutor mic on Start, no remote peer required.
- **Implementation [`9740b1b`](https://github.com/Arangarx/tutoring-notes/commit/9740b1b) + fold [`3bf3a7e`](https://github.com/Arangarx/tutoring-notes/commit/3bf3a7e):** additive `LifecycleInputs.inPersonMode?` + step-3b in `lifecycle-machine.ts` (after `!syncEnabled`, before `!networkOk`); `derivePresentation` guard; two WWC call-site lines. **No engine rewrite.**
- **Teeth:** 4 jest units (authoritative); Playwright `wb-in-person-audio-start.spec.ts` in **`wb-in-person-unmasked`** project (port 3101, no solo env flag) â€” **not** `wb-regression` (solo flag masks â†’ synthetic green).
- **5-axis:** SHIP-WITH-FIXES; SHOULD-FIXes folded (`clock_start` log `mode=`, spec enrollment fix).
- **Owed:** relay run for `wb-in-person-unmasked` at attended merge boundary.

### WS-X BUG-3 â€” PDF stroke leak resolved

- **Root cause:** stale scene merged via `applyRemoteToCanvas` during post-page-switch fingerprint window; v3 broadcast tombstone rebroadcast class (filter-isDeleted on broadcast was **rejected** â€” breaks erasure propagation).
- **Fix [`ef5fb1a`](https://github.com/Arangarx/tutoring-notes/commit/ef5fb1a):** additive guard â€” `onTargetReadTime` also requires `!pageSceneSetFingerprintRef.current.has(targetId)` â†’ falls back to clean `pageDataRef[targetId]` during window. Prerequisite infra from WIP branch (`pageSceneSetFingerprintRef`, stale-onChange rejection). **No v3 broadcast filter change.**
- **Teeth:** `wb-e2-apply-remote-pdf-stroke-leak.spec.ts` + `__WBX_*` seams (prod-inert double-gate).
- **5-axis:** **CLEAN** â€” no over-suppression; tombstone/erase path unchanged.
- **Owed:** relay red/green at attended `test:wb-sync`.

**Serialize rule preserved:** in-person, WS-X, and WS-P deliverable-2 all touch WWC â€” never two code-writers at once.

---

## Settled Andrew decisions (2026-07-05)

Resolved FOR-ANDREW batch â€” treat as facts, not open questions:

| Topic | Resolution |
|---|---|
| **IN_PERSON audio** | Start recording on Start without remote peer (`inPersonMode` boolean; LIVEâ†’IN_PERSON mid-session toggle N/A â€” mode fixed at creation) |
| **WS-K/G tuning** | No pre-flush; 5-chunk/2min debounce; full reduce; libopus re-encode; cap 400; duration free-ride |
| **WS-G-A** | **POLL** â€” "preparing seamless replayâ€¦" when concat ready (follow-up, not blocking core) |
| **WS-N4** | Defaults ratified; NO concurrent-tab End-block |
| **WS-J** | Nearest/5 + `America/Denver`; IN_PERSON wall-elapsed incl. pauses; â‰¥1-increment min; prod migration apply = merge HARD STOP |
| **WS-P-A/B** | Tutor-only defer; read-only-of-FSM `useEffect` in WWC approved â†’ deliverable 2 unblocked |
| **WS-X** | Fix (a) fingerprint-guard approved (filter-isDeleted reversal accepted) â€” **shipped `ef5fb1a`** |
| **Known-issues** | IN-APP (Help/Settings); internal WS-* appendix excluded |
| **`.env` â†’ preview-dev** | Informational; prod verified clean for WS-K/G/J migrations |

---

## Merge-gate items owed (before master)

1. **`npm run test:wb-sync`** once on integrated tip â€” branch cumulatively touches whiteboard/apply-adjacent surfaces. **Component proofs (2026-07-06):** A2 WS-P defer-reload seam âœ… **DISCHARGED** (committed [`4b085db`](https://github.com/Arangarx/tutoring-notes/commit/4b085db)); in-person `wb-in-person-unmasked` âœ… **PROVEN** (A3); **WS-X `wb-e2-apply-remote-pdf-stroke-leak` âŒ RED (A4) â€” deferred, NOT proven**. Full-suite `test:wb-sync` still owed on integrated tip.
2. ~~**`npx next build`** â€” build-surface touched (`next.config.ts` WS-P).~~ **âœ… DISCHARGED 2026-07-05** â€” exit 0 on code-tip `2c7a7bd`/`21378c9` (no TS/ESLint errors; `/api/version` + `/admin/settings/billing` present). Re-run only if the tip advances with further build-surface edits.
3. **WS-M** â€” two-device real-hardware A/V smoke (jsdom cannot verify tutor hears student).
4. **WS-A F-1** â€” outbox register attempt cap (~10 lines) + own 5-axis (SHOULD-FIX deferred from `234c6d7` review).
5. **Migrations** â€” WS-K/G/J additive nullable authored; applied on preview-dev only; **prod apply = Andrew greenlight** at cut.
6. **Andrew hardware re-smoke** â€” master-cut smokebook [`go-to-sarah-master-cut-smokebook.md`](go-to-sarah-master-cut-smokebook.md).

**HARD STOPS:** merge to master; Neon/prod migrations; account reset; force-push.

**Harness note:** Playwright workers 14â†’4 recommended (contention resolved at w=4 on `c2ca8f5`); not yet applied to config.

---

## Ship-to-Sarah gate (governing)

Andrew wants Sarah on `v1-redesign` once waiting room â†’ WB â†’ end is stable for tutor **and** student â€” backend pipeline included. Capture: [`sarah-pilot-feedback-2026-06-16-orchestrator-report.md`](sarah-pilot-feedback-2026-06-16-orchestrator-report.md).

**Confirmed gate items:** per-chunk notes only (no legacy monolithic); End/Continue never silently deletes recording; single-segment seek at every review entry; consent UI honesty (`CONSENT-HONESTY-SARAH-MERGE-BLOCKER`). Deferral ledger: [`pre-master-smoke-deferral-ledger-2026-06-16.md`](pre-master-smoke-deferral-ledger-2026-06-16.md).

Sarah remains on production `master` until gate passes.

---

## Parked / deferred (not blocking next tranche)

| Item | Notes |
|---|---|
| **E3 reconnect pill** | PARKED â€” conflicts with Andrew 2026-07-03 park of `a962171`; BUG-8/BUG-9 fragile A/V; needs hardware |
| **Reachability branch** | `wb-av-reachability-detection-fix` @ `a962171` â€” revisit only if base at risk |
| **WS-U-FRAGILE 1.2** | SMOKE-BLOCK-1 dead Start â€” peer-mesh/presence |
| **Post-Sarah** | SMOKE-NOTES-2 live notes display; SMOKE-UX-3 Â±10s scrub; perspeaker-C runtime wiring; eval harness |
| **SEC** | `tutor-asset/route.ts` any-origin blob URL â€” pre-existing; backlog |

---

## How we work (pointers)

- **Orchestration:** [`AGENTS.md`](../../AGENTS.md) Â§ Model usage protocol; dispatch boundary [`.cursor/rules/orchestrator-discipline.mdc`](../../.cursor/rules/orchestrator-discipline.mdc)
- **Conductor tier:** Opus for fragile durability design + judgment; Composer 2.5 executes; Sonnet 5-axis on fragile diffs. **Fragile-serial** in one worktree.
- **Merging:** smokeable branch â†’ Andrew smoke â†’ `merge --no-ff`; WB sync at merge boundary; build-surface â†’ `npx next build`
- **Smokebooks:** [`SMOKEBOOK-TEMPLATE.md`](SMOKEBOOK-TEMPLATE.md); preview URL via Vercel MCP (never guessed)
- **Process:** preview links in pairs (Vercel `branchAlias` + `preview.usemynk.com` when repointed); behavior tests to spec not code; swap chats ~60â€“70% context

---

## Reading list

Fresh orchestrator â€” read in order:

1. [`AGENTS.md`](../../AGENTS.md)
2. **This file** â€” HEAD first
3. [`wb-wave5-execution-queue.md`](wb-wave5-execution-queue.md) â€” wave-5 backlog
4. [`go-to-sarah-master-cut-plan.md`](go-to-sarah-master-cut-plan.md) â€” executor spec
5. [`part2-test-buildout-plan.md`](part2-test-buildout-plan.md) â€” Part-2 test batches
6. [`docs/LIVE-AV.md`](../LIVE-AV.md) â€” before A/V / per-speaker
7. [`docs/RECORDER-LIFECYCLE.md`](../RECORDER-LIFECYCLE.md) â€” before FSM/outbox/end-session
8. [`docs/WHITEBOARD-STATUS.md`](../WHITEBOARD-STATUS.md)
9. [`docs/BACKLOG.md`](../BACKLOG.md)
10. [`docs/RELEASE-ROADMAP.md`](../RELEASE-ROADMAP.md)

---

## History / audit trail

Updated in place; `git log -p docs/handoff/ORCHESTRATOR-STATE.md`. Heavy-restructure template: [`orchestrator-state-template.md`](orchestrator-state-template.md). Commit truth: `git log --oneline -30 wb-wave5-polish`.

**2026-07-05 crash recovery (resolved):** Andrew's machine crash zeroed loose ref `refs/heads/wb-wave5-polish` (41 null bytes); recovered via reflog to `970aa18`, no committed work lost. CRLF-flip artifact on `EndedUnsavedSessionsList.tsx` later reverted; tree clean.
