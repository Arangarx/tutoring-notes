/**
 * WS-C feature flags — legacy client auto-end on ?intent=endreview.
 * Set false once server-side finalize (C1) is proven via Playwright.
 */
export const LEGACY_INTENT_ENDREVIEW_AUTO_END = false;
